\chapter{Results}
\label{chap:results}

This chapter presents the empirical findings of the CTB study. They reveal a
clear separation between structural conformance, historical timing fidelity,
and scenario responsiveness. The sequential Petri net covers almost all
held-out visits, whereas the simulator reproduces only parts of the observed
timing behavior and systematically underestimates truck turnaround. Adding
static or native workload state does not improve validation fidelity enough to
replace the parsimonious historical reference. Native queue state produces an
internal stress response only under substantially increased demand, not under
the initial 20\,\% intervention. This distinction frames the interpretation
of all subsequent results.

The evidence is presented from model foundation to operational application.
The temporal partitions and control-flow analysis first establish the common
evaluation basis. The historical-fidelity and feature-source comparisons then
show which aspects of the later period are reproduced and which remain biased.
Analyses of temporal transfer, context, arrivals, resources, transition times,
and accumulated time burden locate the main explanatory boundaries without
claiming unobserved causes. The chapter closes with the T22-closure,
increased-demand, and saturation experiments, separating reproducible simulator
responses from physically validated terminal effects.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Temporal Train--Validation--Test Split}
\label{sec:results_eventlog_char}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The temporal split contains 89,460 cases. The first 57,254 cases and 175,902
events form the 64\,\% discovery partition. The following 14,314 cases and
44,146 events form the 16\,\% validation partition, beginning at
\text{2026-04-10 15:50}. Together, these partitions constitute the 71,568-case
development log. The next arrival at \text{2026-04-20 18:17} starts the
untouched 20\,\% test log with 17,892 cases and 55,360 events. The development
log is used again only after configuration selection, when the selected
historical-fidelity model is rediscovered before its single final test
evaluation.
Table~\ref{tab:activity_counts_split} gives the activity counts.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Activity} & \textbf{Train} & \textbf{Test} & \textbf{Total} \\
\midrule
Gate In       & 71,568 & 17,892 & 89,460 \\
Gate Out      & 71,568 & 17,892 & 89,460 \\
RMG\_receive  & 32,794 & 7,171  & 39,965 \\
RMG\_delivery & 17,823 & 4,681  & 22,504 \\
RMG\_mixed    & 3,986  & 953    & 4,939  \\
LL\_delivery  & 9,612  & 3,618  & 13,230 \\
HO2\_delivery & 5,775  & 1,449  & 7,224  \\
HO2\_receive  & 4,188  & 1,276  & 5,464  \\
HO2\_mixed    & 749    & 276    & 1,025  \\
LL\_receive   & 1,932  & 141    & 2,073  \\
LL\_mixed     & 53     & 11     & 64     \\
\bottomrule
\end{tabular}
\caption{Activity counts on the combined development and final test partitions.}
\label{tab:activity_counts_split}
\end{table}

All activities also occur in the test log, but their frequencies change.
\texttt{LL\_delivery} rises from 0.134 to 0.202 events per case.
\texttt{RMG\_receive} falls from 0.458 to 0.401, and
\texttt{LL\_receive} falls from 0.027 to 0.008. This shift is kept in the
evaluation. It contributes to the final yard-activity-rate L1 error in
Table~\ref{tab:final_kpis}. A random split would mix this later-period shift
into all partitions. The temporal design preserves it while retaining a
separate selection and test period. It still covers only one later CTB period.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Control-Flow Stability Across the Held-Out Period}
\label{sec:results_process_discovery}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The raw Inductive-Miner result is informative but is not sufficient as an executable CTB source model. On the 64\,\% discovery partition it contains
three parallel operators because frequently reversed sequential relations are
interpreted as concurrency. It also permits a complete yard bypass. Both
properties conflict with the terminal-expert case contract. The model used by all four ProSiT configurations is therefore the transparent repair defined in
Equation~\ref{eq:ctb_control_flow_contract}. It has 4 places, 12 transitions,
24 arcs, and no parallel operator. It requires at least one yard activity and
permits arbitrary sequential orders and repetitions. Table~\ref{tab:conformance_train_test}
reports the quality of this repaired source model after rediscovery of the
selected parameter configuration on the combined development period.

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Metric} & \textbf{Train} & \textbf{Test} \\
\midrule
Log fitness                      & 1.000000 & 1.000000 \\
Fitting traces (\%)              & 100.000 & 100.000 \\
Average trace fitness            & 1.000000 & 1.000000 \\
Token-based precision            & 0.7815 & 0.7641 \\
Generalisation                   & 0.9762 & 0.9488 \\
Simplicity                       & 0.5000 & 0.5000 \\
\bottomrule
\end{tabular}
\caption{Conformance and structural descriptors of the expert-repaired
sequential source model on development and final test data.}
\label{tab:conformance_train_test}
\end{table}

The test log contains 53 variants, ten of which are absent from development and
together account for ten of 17,892 cases (0.0559\,\%). Unlike a prefix trie,
the repaired loop can replay unseen sequential combinations. It fits all
17,892 test cases completely. This is substantive test evidence rather than a
consequence of memorising the construction log.

The remaining measures show the expected abstraction trade-off. Test precision
is 0.7641 because the loop permits more sequential combinations and
repetitions than were observed. This broader language is consistent with the
stated CTB contract because experts identified no forbidden yard successor
pair. Generalisation remains high at 0.9488. A hard operator check confirms
zero parallel cuts, and the ten historical-reference test replications produce neither
within-case overlap, gate-only cases, nor a \texttt{Gate Out} before the final
yard activity.

\medskip

\noindent\textbf{RQ2 answer.} Automatic discovery identifies a compact and
general control-flow language, but the raw result is not physically admissible
without domain repair. The expert-repaired source model achieves complete test
fitness, preserves sequential generalisation, and satisfies the mandatory
gate, yard-work, and no-concurrency conditions. The result supports the
repaired model as a simulation backbone. It does not support presenting the
unmodified Inductive-Miner output as a fully automatic operational model.

\subsection{Discovery-to-Execution Repair}

Transitioning from the raw tree to the executable tree is a primary modeling
step, rather than a secondary robustness test. It removes both forms of excess
behavior identified by the domain audit, consisting of the concurrent activities of one single truck
and the gate-only bypass. At the same time, it retains the activity alphabet
and deliberately allows arbitrary sequential yard combinations and
repetitions. The repair is fixed before validation and is identical for every
feature-source configuration. It therefore cannot explain performance
differences among the four ProSiT cells.

The raw tree itself remains an auditable discovery artifact. Its structure is
also sample-window sensitive, as the 64\,\% discovery tree contains three
parallel operators, when the tree rediscovered on the complete 80\,\%
development period contains none but still allows for the yard bypass. This
instability enhances the methodological finding that a high-fitness
discovered petri net is not automatically a physically valid simulation model.

\paragraph{Interpretation of the \texttt{LL\_mixed} Branch}

The special structural role of \texttt{LL\_mixed} is supported by a small and
highly concentrated set of visits. Across the complete train and hold-out
periods, 64 visits contain the activity and 57 of them contain no other yard
activity. Their observed pattern is therefore simply \texttt{Gate In},
\texttt{LL\_mixed}, and \texttt{Gate Out}. Only seven visits combine
\texttt{LL\_mixed} with another yard activity. In six training visits it occurs
first and is followed by \texttt{RMG\_receive}. The single held-out exception
follows the order \texttt{RMG\_delivery}, \texttt{LL\_mixed}. A plausible
operational interpretation is that an empty-container exchange is normally
completed before another yard area for routing reasons. The six training cases
are compatible with that explanation, but the small sample and held-out
exception do not justify a mandatory ordering rule. The isolated branch is
mainly evidence of an valid one-stop process variant rather than evidence
that \texttt{LL\_mixed} must always precede other yard work.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Historical Fidelity and Model Screening}
\label{sec:results_holdout_fidelity}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

This section contains two distinct stages. RQ3 compares the four feature-source
configurations on the 16\,\% validation period and selects one configuration by
the pre-specified rule. RQ1 then evaluates that configuration, rediscovered on
the combined 80\,\% development log, once on the untouched test period. All
configurations use the same expert-repaired Petri net, arrival calibration and
ten-seed protocol. Their difference is therefore the way routing, duration and
resource behavior use contextual information.

\subsection{RQ1: Historical Fidelity of the Data-Aware Reference Model}

The selected cap-three \textit{visit-only} historical-fidelity model uses the
expert-repaired
sequential net, native ProSiT routing learned from development data, the
minute-resolved arrival model, and non-pre-emptive completion. The raw RMG
capacities are capped at three per block. This cap comes from the domain, not
from the hold-out split. The hold-out is used only for evaluation.

Ten runs of 17,892 cases use seeds 42--51. Each run starts at the frozen cutoff
$t_{\text{start}}=\text{2026-04-20 18:17}$. Table~\ref{tab:final_kpis}
summarises the comparison. Its intervals cover seed variation for this fixed
model and hold-out only.

\begin{table}[htbp]
\centering
\setlength{\tabcolsep}{3.4pt}
\begin{tabular}{L{4.6cm}rrrr}
\toprule
\textbf{KPI} & \textbf{Real/ref.} & \textbf{Simulated} &
\textbf{Difference} & \textbf{95\,\% MC CI} \\
\midrule
Yard service EMD, unweighted (min) & -- & 3.150 & -- & [2.985, 3.315] \\
Yard service EMD, real-frequency weighted (min) & -- & 2.341 & -- & [2.295, 2.387] \\
Case-turnaround EMD (min) & -- & 6.159 & -- & [6.041, 6.278] \\
Case-turnaround KS & -- & 0.0928 & -- & [0.0893, 0.0962] \\
Inter-arrival EMD (min) & -- & 0.0724 & -- & [0.0674, 0.0774] \\
Inter-arrival KS & -- & 0.0254 & -- & [0.0215, 0.0292] \\
N-Gram Distance ($N=3$) & -- & 0.05677 & -- & [0.05540, 0.05813] \\
Mean turnaround (min) & 39.742 & 33.812 & $-5.930$ & [$-6.060$, $-5.799$]$^{a}$ \\
P90 turnaround (min) & 71.000 & 57.900 & $-13.100$ & [$-13.326$, $-12.874$]$^{a}$ \\
Simultaneous-arrival share (\%) & 56.660 & 54.221 & $-2.439$ pp & [$-2.625$, $-2.253$] pp$^{a}$ \\
Yard-activity-rate L1 error & -- & 0.1636 & -- & [0.1593, 0.1678] \\
\bottomrule
\end{tabular}
\caption{Held-out fidelity of the selected cap-three \textit{visit-only}
historical-fidelity model
($N_{\text{seeds}}=10$, seeds 42--51, and 17,892 cases per run).}
\label{tab:final_kpis}
\smallskip
\parbox{0.95\textwidth}{\footnotesize $^{a}$The real hold-out statistic is
treated as fixed, so the interval refers to the simulated-minus-real
difference. Intervals for the remaining rows refer to the named error metric
across simulated logs. Gate In and Gate Out are excluded from both yard-service
EMD aggregates.}
\end{table}

The result is mixed. Inter-arrival spacing is reproduced closely, and the yard
service mismatch is moderate. Frequency weighting reduces the latter because
some of the larger errors occur in rare activities
(Table~\ref{tab:yard_activity_emd}). The complete visit is less accurate. Mean
turnaround is 5.930 minutes too short, or 14.92\,\%. P90 is 13.100 minutes too
short, or 18.45\,\%. This discrepancy persists across the ten seeds. The narrow
intervals show that it is not caused by ordinary run-to-run simulation
variation. They do not show that the model itself is correct or identify the
source of the error.

The rediscovered Train-80 \textit{visit-only} historical-reference bundle gives
a completion-order N-Gram Distance
(NGD) with $N=3$ of 0.05677 on the same final test period. Reconstructing the
order from the explicit ITS event-order field changes the result only in the
fifth decimal place. The NGD result is therefore not driven by the small number
of timestamp inversions in the underlying data. Its moderate increase relative
to validation is consistent with temporal transfer in local activity
combinations. NGD remains a control-flow distance and is not evidence that
timing, queues, or physical movement are represented correctly.

As a role check, the same test period is also evaluated with the cap-three
\textit{native-only} bundle. Its frequency-weighted yard-service,
case-turnaround, and activity-rate errors remain close to those of
\textit{visit-only}. The small differences do not change the historical
reference decision. They show that the native-state mechanism configuration does not
incur a large additional historical mismatch while enabling a distinct internal
demand-propagation test.

All ten runs pass the case-order checks. They contain no gate-only cases,
within-case overlaps, invalid boundaries, or decreasing completion times. Gate
Out never occurs before the last yard activity. No accepted duration exceeds
24 hours. These checks are necessary, but they do not remove the timing bias.

Figure~\ref{fig:cdf_turnaround} shows the empirical CDF of case turnaround
for the real held-out period and a representative simulated replication. The
simulated distribution is shifted left across the entire range: the deficit is
not confined to the upper tail but reflects a systematic shortfall that grows
with turnaround duration. At the median (real $\approx32$ minutes), the
simulated CDF is already visibly ahead. At the 90th percentile, the gap
widens further, consistent with the 13.100-minute P90 deficit in
Table~\ref{tab:final_kpis}. The upper tail beyond 200 minutes is thin in both
logs, it does not drive the headline metrics.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.82\textwidth]{figures/discovery/cdf_turnaround.png}
\caption{Empirical CDF of case turnaround time for the real held-out period
and a representative simulated replication. The leftward shift of the
simulated curve shows that the turnaround deficit is distributed across the
body of the distribution, not concentrated in the tail.}
\label{fig:cdf_turnaround}
\end{figure}

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Yard activity} & \textbf{Mean EMD (min)} & \textbf{95\,\% MC CI} \\
\midrule
HO2\_delivery & 2.086 & [1.876, 2.296] \\
HO2\_mixed    & 4.440 & [3.655, 5.224] \\
HO2\_receive  & 1.844 & [1.669, 2.018] \\
LL\_delivery  & 1.448 & [1.365, 1.530] \\
LL\_mixed     & 5.266 & [3.968, 6.564] \\
LL\_receive   & 4.265 & [4.053, 4.477] \\
RMG\_delivery & 2.525 & [2.445, 2.605] \\
RMG\_mixed    & 4.007 & [3.666, 4.348] \\
RMG\_receive  & 2.467 & [2.406, 2.528] \\
\bottomrule
\end{tabular}
\caption{Held-out service-time EMD by yard activity for the cap-three
\textit{visit-only} historical reference ($N_{\text{seeds}}=10$).}
\label{tab:yard_activity_emd}
\end{table}

\noindent\textbf{RQ1 answer.} The model reproduces the case order and parts of
the arrival and service-time behavior. It does not reproduce the full
turnaround distribution. Total visit time is systematically too short.

\subsection{RQ3: Feature Sources and Validation Fidelity}

The $2\times2$ experiment tests whether either of the two additional information
sources improves validation fidelity relative to the workload-blind
\textit{visit-only} configuration. Table~\ref{tab:configuration_screening}
reports the mean errors on the 16\,\% validation period. All configurations use
the same discovery partition, repaired Petri net, discovery settings, and
replication protocol defined in Section~\ref{sec:simulation_parameter_discovery}.
They differ only in the availability of manually constructed static
demand/utilisation proxies and ProSiT's native workload and queue state.
Lower values indicate better fidelity.

\begin{table}[htbp]
\centering
\small
\setlength{\tabcolsep}{3.0pt}
\begin{tabular}{L{4.2cm}rrrr}
\toprule
\textbf{Metric} & \textbf{visit-only} & \textbf{static-only} &
\textbf{native-only} & \textbf{both} \\
\midrule
Turnaround EMD (min) & 9.149 & 9.163 & 9.253 & 9.173 \\
Yard-service EMD, weighted (min) & 1.735 & 1.760 & 1.773 & 1.715 \\
Yard-activity-rate L1 error & 0.1285 & 0.1305 & 0.1291 & 0.1305 \\
NGD ($N=3$) & 0.04857 & 0.04915 & 0.04863 & 0.04884 \\
\bottomrule
\end{tabular}
\caption{Mean validation errors in the $2\times2$ feature-source experiment
($N_{\text{seeds}}=10$ and 14,314 cases per run).}
\label{tab:configuration_screening}
\end{table}

The results provide no clear historical-fidelity gain from either additional
state source. The static-only configuration is slightly worse than
\textit{visit-only} on all four primary measures. The \textit{native-only}
configuration is also close to the reference: turnaround EMD increases by
0.105 minutes and weighted yard-service EMD by 0.037 minutes, while the
differences in activity-rate L1 and NGD are only 0.0006 and 0.00006,
respectively. The combined configuration has the lowest mean weighted
yard-service EMD, at 1.715 rather than 1.735 minutes, but does not improve the
other three measures.

The paired seed comparisons lead to the same conclusion. For every candidate
configuration and primary metric, the 95\,\% paired Monte-Carlo interval
includes zero. None of the added-state configurations therefore satisfies the
pre-specified selection rule of showing a resolved improvement without a
resolved deterioration elsewhere. This result should not be interpreted as
statistical equivalence between the configurations, but rather the intervals quantify
simulation-level variation across the ten seeds and no equivalence margin was
defined.

The NGD comparison is also insensitive to the event-order definition.
Recomputing the four configurations using the explicit
\texttt{case:event:order} field changes the mean values only in the fifth
decimal place and does not change the comparison. All configurations also
satisfy the common sequential case contract, so structural validity does not
distinguish the four cells.

The absence of an aggregate fidelity improvement does not mean that the added
predictors are ignored by ProSiT. The static-only and combined bundles retain
several splits on static workload variables, including
\texttt{target\_rank}, \texttt{target\_demand}, and
\texttt{rmg\_demand}. The native-only and combined bundles each retain one
\texttt{queue\_length} split. Most fitted rules nevertheless continue to use
the visit, process, and location attributes that are common to all four
configurations. The additional information is therefore used locally by the
fitted trees, but this does not translate into better generated-log fidelity
on the validation period.

Individually retained splits here, should not be interpreted as unique effects of
specific predictors.Several static workload variables are strongly correlated: Pearson $r$ is
0.922 between gate and RMG utilisation, 0.898 between gate and RMG demand,
and $-0.790$ between target utilisation and target rank. This does not
invalidate their use as predictors in the decision trees, but it limits the
interpretation of individual retained splits. Correlated predictors may
substitute for one another, so the selected split can depend on the available
candidate set rather than identify a unique effect of one variable \cite{SimulationGuidedRepair2025}. This
distinction is important here because RQ3 evaluates the contribution of the
static information source as a bundle, rather than the independent effect of
individual workload variables.

The magnitude of the differences is also small. For example,
\textit{native-only} changes turnaround EMD by only 0.105 minutes and weighted
yard-service EMD by 0.037 minutes, while its NGD differs from
\textit{visit-only} by only 0.00006. The four configurations therefore generate
very similar historical behaviour despite having different workload information
available to their fitted rules. For RQ3, this supports the parsimonious
\textit{visit-only} reference. More broadly, however, the weak separation shows
that the added workload state has little influence on historical output under
the observed operating conditions. This may reflect limited predictive signal,
limited use of the added state by the fitted trees, or an operating range in
which workload effects are not strongly expressed. The validation experiment
alone cannot distinguish between these explanations.
s rather than as evidence for or against individual variables.

\medskip

\noindent\textbf{RQ3 answer.}
Under the tested ProSiT specification, neither the manually constructed
workload proxies nor ProSiT's native dynamic state improves historical
validation fidelity enough to justify the additional state under the
pre-specified selection rule. \textit{Visit-only} is therefore selected as the
historical-fidelity reference. This result concerns reproduction of the
observed process only. If an endogenous workload state is additionally required for a
specific what-if intervention is evaluated separately under RQ4.

\paragraph{Meaning of the Time Measures}
\label{sec:results_temporal_note}

The real log has start and completion timestamps, but no enablement timestamp.
Service-time EMD uses $\text{complete}-\text{start}$. Turnaround EMD uses
$\text{last\_complete}-\text{first\_start}$. Both can be calculated for both
logs. ProSiT derives structural enablement internally and writes it only to the
simulated log. In the sequential CTB net, every activity after Gate In becomes
enabled when its preceding visible activity completes. Simulated
$\text{start}-\text{enabled}$ can therefore be compared with the observed gap
$\text{start}_{i}-\text{complete}_{i-1}$. The comparison evaluates composite
pre-service fidelity. It cannot validate physical readiness or real queueing
time because neither is observed.

The existing ten-seed baseline replications show that the composite gap is
systematically too short for the two dominant RMG handling patterns. For
\texttt{RMG\_receive}, the observed mean is 13.974 minutes and the simulated
mean is 8.224 minutes, a difference of $-5.749$ minutes (95\,\% MC CI
$[-5.808,-5.691]$). For \texttt{RMG\_delivery}, the corresponding means are
12.140 and 8.100 minutes, a difference of $-4.040$ minutes
($[-4.109,-3.972]$). These component results are consistent with the overall
turnaround underprediction. They assess the complete structural gap and do not
show which share is movement, traffic, dispatching, or resource waiting.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Temporal Transfer and Error Decomposition}
\label{sec:results_temporal_transfer}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The observed process becomes slower between the training and test periods.
Mean truck turnaround increases from 37.293 to 39.742 minutes, a
2.45-minute increase (95\,\% Welch CI $[2.003,2.896]$). The median rises
from 29 to 32 minutes and P90 from 67 to 71 minutes. The Wasserstein distance
between the two observed turnaround distributions is 3.24 minutes. Yard
service changes in the same direction: mean duration increases for all nine
yard activities, with a frequency-weighted mean increase of 2.120 minutes.
The test period therefore represents a measurably different operating period,
rather than a unchanged continuation of the training data.

Changes in the available workload proxies do not provide a simple congestion
explanation for this shift. Gate and RMG utilisation proxies are lower in the
test period while yard-service times and complete turnaround are higher.
These variables should therefore be interpreted as empirical predictors, rather
than stable measurements of physical congestion. Simulation
models discovered from historical logs may lose accuracy when the underlying
process evolves. Vinci et al.\ address this problem through online discovery
that gives greater weight to recent observations while retaining historical
information \cite{vinci2025onlinediscoverysimulationmodels}.

The change in observed conditions explains only part of the final turnaround
error. For the cap-three \textit{visit-only} historical reference,
\begin{equation}
39.742-33.812
=
(37.293-33.812)
+
(39.742-37.293)
=
3.481+2.450
=
5.930\ \text{minutes}.
\end{equation}
Thus, 3.481 minutes, or about 59\,\% of the mean test-period deficit, is already
present relative to the training-period mean. The remaining 2.450 minutes,
or about 41\,\%, corresponds to the increase in observed mean turnaround
between the two periods. This is an accounting identity for the means, not a
causal decomposition and not a decomposition of EMD.

Temporal transfer therefore contributes substantially to the test-period
turnaround error, but it does not explain the full bias. The simulator already
underestimates mean turnaround relative to the period from which it was
learned, indicating a model limitation in addition to the later process change.
This distinction is important because direct comparison with a future event log
can otherwise mix simulation error with changes in the underlying process
\cite{Ozdemir2025UtilityBPS}. Recent work on temporal deviations in BPS similarly
argues for moving beyond the magnitude of simulation--reality differences and
examining the arrival, processing-time, and resource inputs associated with
them \cite{Wang2026TemporalDeviations}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Context Signal in Activity Durations}
\label{sec:results_per_activity}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The RQ3 ablation shows no resolved validation improvement from adding the
available workload state. The next diagnostic
checks how much duration information is present in the
available attributes. Table~\ref{tab:tree_r2_train_test} reports a
training-only decision-tree test. It is not a breakdown of the final EMD. It
also cannot prove why the RQ3 result occurs.

\begin{table}[htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Activity} & \textbf{$R^{2}$} \\
\midrule
LL\_delivery  & 0.263 \\
LL\_mixed     & 0.258 \\
HO2\_receive  & 0.115 \\
HO2\_mixed    & 0.070 \\
RMG\_mixed    & 0.051 \\
HO2\_delivery & 0.029 \\
LL\_receive   & 0.024 \\
RMG\_receive  & $-0.005$ \\
RMG\_delivery & $-0.004$ \\
Gate In       & 1.000 \\
Gate Out      & 1.000 \\
\bottomrule
\end{tabular}
\caption{Per-activity duration $R^{2}$ of a decision-tree model
conditioned on target demand, target utilisation, target rank, visit
complexity, receive/delivery counts, hour, weekday and weekend
indicator (training log, grid search over depth/leaf, best per
activity).}
\label{tab:tree_r2_train_test}
\end{table}

Gate activities have $R^{2}=1.000$ because their service duration is fixed at
zero. This is not useful predictive performance. The nine yard activities
range from $-0.005$ for \texttt{RMG\_receive} to $0.263$ for
\texttt{LL\_delivery}. The two main RMG activities are close to zero. RMG
activities still account for more than 60\,\% of yard events.

The available attributes explain little of the duration variation in the
largest activity family. This is consistent with the weak RQ3 result. It is
not proof of its cause. Leaf size, fitted distributions, routing, resource
selection, and the change between train and test may also affect the result.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Arrival Structure as a Counterexample}
\label{sec:results_arrival_process}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The weak duration results do not mean that context is useless in every sense, as
the arrival behavior provides a counterexample. Table~\ref{tab:arrival_benchmark} compares
one pooled arrival rate with a tree using hour, weekday, and weekend features.
The test uses a chronological 80/20 split inside the training partition. It is
a training diagnostic, not another held-out validation result.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Model} & \textbf{MAE} & \textbf{RMSE} & \textbf{$R^{2}$} \\
\midrule
Pooled arrival rate      & 28.98 & 34.60 & $-0.001$ \\
Time-aware decision tree & \textbf{9.41} & \textbf{16.87} & \textbf{0.762} \\
\bottomrule
\end{tabular}
\caption{Arrival-count benchmark on 30-minute buckets (training log only,
chronological 80/20 split).}
\label{tab:arrival_benchmark}
\end{table}

The time-aware tree reduces MAE by 67.5\,\% and RMSE by 51.2\,\%. The main
pattern is also visible in Table~\ref{tab:day_night}. The estimated day rate is
about 8.7 times the night rate. Arrival intensity is not constant, so the time conditioning is useful here. The result does not show that the tree
captures every source of arrival variation.

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Period} & \textbf{Mean inter-arrival (min)} &
\textbf{Implied mean arrivals/hour} \\
\midrule
All   & 1.00 & 60.2 \\
Day   & 0.54 & 110.9 \\
Night & 4.69 & 12.8 \\
\bottomrule
\end{tabular}
\caption{Day and night arrival statistics on the training log.}
\label{tab:day_night}
\end{table}

The low inter-arrival EMD in Table~\ref{tab:final_kpis} comes from the reported
calibrated historical simulation, not from this tree alone. Good arrival
timing does not fix the turnaround bias tho, as the missing time occurs after arrival.

A qualification is necessary. The inter-arrival EMD compares the marginal
distribution of successive arrival gaps.  It does not validate the temporal
structure of the arrival process: hour-of-day and weekday profiles, arrival
bursts, temporal autocorrelation, or conditional relationships between arrival
time, visit type, and processing duration are not assessed by this single
distributional distance.  A simulator can reproduce the basic inter-arrival
distribution while generating arrivals at the wrong times of day or with the
wrong peak behavior.  This is especially relevant in a logistics process
with strong calendar effects.  The reported inter-arrival EMD should therefore
be interpreted as a necessary but insufficient as a stand alone condition for arrival-process
fidelity.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Resource Discovery and the Domain Cap}
\label{sec:results_resource_capacities}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The discovered source bundle contains 26 resource identifiers.
Table~\ref{tab:capacities_train80} shows their maximum concurrency. The source
bundle is kept for audit. The reported runs use a copy in which each RMG block is
capped at three, a value confirmed by the project's terminal experts. All other
parameters stay unchanged. The earlier pool-level overlap report is only a
diagnostic, and not the simulator configuration.

\begin{table}[htbp]
\centering
\begin{tabular}{L{1.3cm}L{3.1cm}L{4.0cm}L{2.6cm}}
\toprule
\textbf{Family} & \textbf{Identifiers} & \textbf{Eligible activities} &
\textbf{Maximum concurrency} \\
\midrule
Gate In  & Res.GateIn  & Gate In & 1 \\
Gate Out & Res.GateOut & Gate Out & 1 \\
HO2      & HO2 & three HO2 activities & 15 \\
LL       & LL  & three LL activities  & 22 \\
RMG      & T06--T27 (22 blocks) & three RMG activities & 4--5$^{a}$ \\
\bottomrule
\end{tabular}
\caption{Resource representation in the freely discovered calibrated
source bundle before applying the expert-validated RMG cap.}
\label{tab:capacities_train80}
\smallskip
\parbox{0.94\textwidth}{\footnotesize $^{a}$Ten RMG block identifiers
show a maximum concurrency of four and twelve show a maximum concurrency of
five. Their aggregate upper bound is 100 simultaneous RMG tasks.
This exceeds the expert-validated physical upper bound of 66 implied by at
most three RMGs at each of the 22 blocks.}
\end{table}

After discovery, the CTB calibration replaces the resource-specific calendars,
which contain 123--128 active hourly slots, with 168 open slots per week. This
deliberate post-discovery setting lets trucks admitted before a gate closure
finish without pausing their activities. The unchanged arrival calendar still
controls admission and therefore retains the gate-closure schedule. The open
resource calendars do not establish continuous physical staffing. Every RMG
activity can select any of the 22
block identifiers. The learned rules provide the weights. Removing T22 leaves
21 identifiers and removes three capacity units from the effective
\textit{native-only} execution bundle.
The HO2 and LL concurrency values, fully open CTB-calibrated resource calendars, and
interchangeable-block assignment mechanism were not covered by the expert
confirmation and remain model abstractions. The comparatively high discovered
concurrency values for HO2 and LL are retained because the available terminal
resource information for these areas is too volatile to support a stable
physical capacity estimate. Both areas also contain a varying number of
handling positions that allow several trucks to be processed or
held in parallel. The discovered concurrency therefore represents this
aggregate parallel handling capacity rather than a verified count of individual physical resources.

The discovered raw RMG capacities sum up to 100, whereas the expert-validated ceiling of
three per block gives an aggregate upper bound of 66 across the 22 blocks. The
discovered values describe overlapping event intervals, not verified crane
counts. The cap removes an implausible concurrency value but does not assert
that all 66 units are simultaneously staffed or available to landside trucks.

This is the material expert-supported domain correction in the executable parameter bundle.
Preserving the ITS schedule-derived XES order and checking for parallel operators corrects and
guards the discovery input without replacing the discovered control flow. The
resource cap, by contrast, changes the learned capacities to enforce a stated
physical upper bound. The reported simulator therefore combines an automatically
discovered Petri net and parameters with one explicit resource-domain
constraint.

An RMG block is still only a selectable resource label. It is not container
location state. After T22 is removed, the model can assign work elsewhere
without checking storage or travel feasibility. This limit is central to
Scenario~A and is discussed in Section~\ref{sec:limitations}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Transition Time Within Composite Pre-Service Delay}
\label{sec:results_percentile_sens}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

An earlier pipeline used route-specific transition-time baselines to
reconstruct enablement, whereas the reported simulations use ProSiT's
model-derived enablement mechanism (Section~\ref{sec:lifecycle_events}). The
estimates are therefore retained only as a scale check. Across the 48
gate-to-yard and yard-to-gate transitions with at least twenty observations,
the fifth-percentile transition times range from 2.16 to 5.28 minutes. The frequency-weighted estimates are 3.88 minutes inbound and 3.75 minutes outbound, or 7.62 minutes per visit. This is comparable in scale to the 5.930-minute mean turnaround underprediction in
Table~\ref{tab:final_kpis}. The comparison indicates that unobserved transition
time may contribute materially to the residual, but it is neither an additive
correction nor a causal decomposition.

Separating movement from waiting is a recognised challenge in data-driven
simulation for physical logistics. Reviews report that queue and resource
behaviour often cannot be recovered from conventional event logs because the
required operational state is missing
\cite{Sulis2026SimulationReview,Grobis2025ProductionLogistics}. A recent
production-logistics application decomposes an observed inter-activity interval
into transport and waiting only where buffer state identifies the two
components. Transport is then represented explicitly, while waiting emerges
from resource and buffer logic \cite{Grobis2026SimulationDevelopment}. CTB does
not provide reliable enablement, trajectory, or queue-state observations for
such a decomposition. ProSiT models execution, model-derived waiting, arrivals, and
resource availability, but provides no separate timed spatial-transfer
primitive \cite{Vinci2026prosit}. Physical transition time is therefore not
identifiable or directly executable within the present model. Introducing a
separate transition component would first require movement to be observed or
estimated and the remaining pre-service distribution to be refitted. Otherwise,
part of the undifferentiated gap would be represented twice. A valid extension
must also introduce explicit movement semantics in the model. Until then, the
output is interpreted as \emph{composite pre-service delay}, not as realistically
measured queueing time.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Held-Out Bottleneck Analysis}
\label{sec:results_bottlenecks}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

A bottleneck analysis identifies long individual activity compared to a
frequent activity at which a large amount of system time accumulates. The
held-out log is therefore decomposed at event level. The recorded handling operations service time
is completion minus start. The non-negative gap from the previous event's
completion to the current event's start is called \emph{composite pre-service
delay}. For the sequential truck process, the sum of both components exactly
reconstructs each case turnaround. The pre-service delay time span can contain
movement, traffic and transfer, dispatching, and queueing. It is not interpreted
as a direct queue measurement in this thesis.

Across the 17,892 held-out visits, mean turnaround is 39.742 minutes. Recorded
handling contributes 14.755 minutes per visit and composite pre-service delay
contributes 24.987 minutes. The decomposition error is zero for every case.
Table~\ref{tab:bottleneck_burden} ranks activities by their frequency-weighted
contribution rather than their mean duration alone.

\begin{table}[htbp]
\centering
\setlength{\tabcolsep}{4pt}
\begin{tabular}{L{3.2cm}rrr}
\toprule
\textbf{Activity} & \textbf{Service (min)} &
\textbf{Composite pre-service (min)} & \textbf{Total share} \\
\midrule
Gate Out     & 0      & 201,690 & 28.36\,\% \\
RMG receive  & 98,808 & 100,205 & 27.99\,\% \\
RMG delivery & 53,028 & 56,829  & 15.45\,\% \\
LL delivery  & 39,868 & 21,651  & 8.65\,\% \\
HO2 receive  & 23,671 & 20,934  & 6.27\,\% \\
\bottomrule
\end{tabular}
\caption{Largest accumulated service and composite pre-service burdens in the
held-out period. Minutes are totals over all 17,892 visits.}
\label{tab:bottleneck_burden}
\end{table}

The largest component is the gap before Gate Out: 11.273 minutes per visit on
average and 201,690 minutes in total. It contains the outbound transition and additionally 
any unpredictibale human behavior regarding intentional break of truck drivers while still on the treminal, or unobserved releases due to customer communications. Identifying the Gate Out operation as a queueing bottleneck
would be unsuitable in the given setting. RMG receive is the largest handling-related accumulation
point. Its mean service is 13.779 minutes and its mean composite pre-service
delay is 13.974 minutes. Together the activity accounts for 27.99\,\% of all
reconstructed held-out time. RMG delivery accounts for a further 15.45\,\%.
These frequency-weighted results make the RMG receive/delivery distinction
operationally important even though the present scenarios do not identify a
resolved turnaround effect.

\texttt{LL\_mixed} is not a system-level bottleneck in this decomposition. Its
eleven held-out occurrences accumulate 233 minutes of service and composite
pre-service delay, only 0.0328\,\% of total held-out turnaround. All are followed
directly by \texttt{Gate Out}. In the training period, the six
\texttt{LL\_mixed}--\texttt{RMG\_receive} transitions have a mean downstream
pre-service gap of 10.667 minutes, compared with 11.251 minutes for
\texttt{RMG\_receive} after other predecessors. Their medians are both six
minutes and their P90 values are 22.5 and 23.0 minutes. The available evidence
therefore does not indicate additional congestion after \texttt{LL\_mixed}.
Its process-structural distinctiveness should not be confused with a large
frequency-weighted time burden.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\textwidth]{figures/results_bottleneck_time_burden.png}
\caption{Accumulated held-out time by activity, separated into recorded
handling service and composite pre-service delay.}
\label{fig:bottleneck_time_burden}
\end{figure}

The resource-overlap audit reinforces the modelling caveat. Several RMG block
labels exhibit four overlapping recorded intervals even though the effective
execution bundle caps capacity at three. The overlap is evidence about
the event-log resource abstraction and timestamp behavior, not proof that a
fourth crane exists. A causal bottleneck claim requires spatially sticky block
assignment and an explicit queue state. The present analysis therefore
identifies where time accumulates and which components deserve further
modelling. It does not attribute all accumulated delay to capacity shortage.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{What-If Experiments}
\label{sec:results_scenarios}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The analysis distinguishes the mechanisms required by the two interventions.
The cap-three \textit{visit-only} bundle remains the historical-fidelity
reference and acts as a workload-blind negative control. For the demand test,
the cap-three \textit{native-only} bundle is the minimal configuration that
contains endogenous queue and workload state. It is used as a mechanism probe,
not as a validated generic scenario model. Both use the same
expert-repaired Petri net, ten matched seeds (42--51), and 17,892 cases per
run. The RQ1 results still come from Table~\ref{tab:final_kpis}.

The repaired sequential net also stays fixed. The experiments can change arrival
timing, resource assignment, and model-derived delays, while the loop may still
generate unseen sequential combinations of the known yard activities. Extra
activity types, detours represented by new events, and unmodeled exception
mechanisms, remain outside the model. All scenario results are conditional on
this discovered control-flow language.

The operational question is not simply, whether a parameter can be changed.
For terminal planning, the relevant question is how a forecast condition and a
candidate response propagate through process executions. For example, whether reducing the number of truck slots released per hour under forecast high
utilisation protects turnaround or whether a resource policy shifts delay from
receive to delivery handling. The scenario KPIs are therefore reported both at
case level and separately for the categorical RMG handling patterns. This makes
the experiments decision-oriented, while keeping the distinction between an
effect inside the simulation and an effect established for the physical
terminal.

\begin{table}[htbp]
\centering
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
\textbf{KPI} & \textbf{Reference} & \textbf{Outcome} & \textbf{Change} & \textbf{95\,\% paired MC CI} \\
\midrule
Mean turnaround (min) & 33.635 & 33.605 & $-0.030$ & [$-0.168$, $+0.107$] \\
P90 turnaround (min) & 57.400 & 57.500 & $+0.100$ & [$-0.428$, $+0.628$] \\
Mean RMG service (min) & 11.028 & 11.035 & $+0.006$ & [$-0.053$, $+0.066$] \\
Mean RMG pre-service (min) & 8.031 & 7.938 & $-0.093$ & [$-0.146$, $-0.040$] \\
RMG receive service (min) & 11.336 & 11.301 & $-0.035$ & [$-0.132$, $+0.062$] \\
RMG receive pre-service (min) & 8.086 & 8.021 & $-0.066$ & [$-0.144$, $+0.013$] \\
RMG delivery service (min) & 8.706 & 8.795 & $+0.089$ & [$-0.011$, $+0.188$] \\
RMG delivery pre-service (min) & 8.001 & 7.873 & $-0.128$ & [$-0.265$, $+0.009$] \\
\bottomrule
\end{tabularx}
\caption{Paired Scenario A results in the \textit{native-only} demand-mechanism
configuration:
T22 resource-pool reallocation
($N_{\text{seeds}}=10$, scenario minus matched baseline).}
\label{tab:scenario_kpis}
\end{table}

\begin{table}[htbp]
\centering
\footnotesize
\setlength{\tabcolsep}{3.0pt}
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
\textbf{KPI} & \textbf{Reference} & \textbf{Outcome} & \textbf{Change} & \textbf{95\,\% paired MC CI} \\
\midrule
Model mean working inter-arrival (min) & 0.6830 & 0.5692 & $-0.1138$ & exact \\
Model arrivals per working hour & 87.842 & 105.411 & $+20.000\,\%$ & exact \\
Observed mean inter-arrival (min) & 0.8282 & 0.7145 & $-0.1137$ & [$-0.1145$, $-0.1128$] \\
Observed arrivals per elapsed hour & 72.452 & 83.979 & $+11.527$ ($+15.910\,\%$) & [$+11.478$, $+11.577$] \\
Observed simultaneous-arrival share (\%) & 54.221 & 60.015 & $+5.795$ pp & [$+5.736$, $+5.853$] pp \\
Mean turnaround (min) & 33.635 & 33.548 & $-0.087$ & [$-0.281$, $+0.106$] \\
Mean RMG service (min) & 11.028 & 11.078 & $+0.049$ & [$-0.041$, $+0.140$] \\
Mean RMG pre-service (min) & 8.031 & 7.969 & $-0.062$ & [$-0.139$, $+0.014$] \\
RMG receive service / pre-service (min) & 11.336 / 8.086 & 11.361 / 8.033 & $+0.025$ / $-0.053$ & [$-0.072$, $+0.123$] / [$-0.159$, $+0.053$] \\
RMG delivery service / pre-service (min) & 8.706 / 8.001 & 8.772 / 7.902 & $+0.065$ / $-0.099$ & [$-0.072$, $+0.203$] / [$-0.234$, $+0.037$] \\
\bottomrule
\end{tabularx}
\caption{Paired Scenario B results in the \textit{native-only} demand-mechanism
configuration:
20\,\% higher working-time arrival intensity
($N_{\text{seeds}}=10$, scenario minus matched baseline).}
\label{tab:demand_scenario_kpis}
\smallskip
\parbox{0.97\textwidth}{\scriptsize Pre-service denotes the model-derived
interval from enablement to activity start, not pure queueing time.
``Observed'' arrival measures use the minute-resolved simulated event log and
elapsed calendar time. Intervals quantify paired seed-wise variation
conditional on the frozen model. All 30 primary logs pass the structural case
contracts, and no service event or complete visit exceeds 24 hours. The
expert-validated RMG ceiling reduces aggregate maximum concurrency from the
discovered 100 to 66.}
\end{table}

\subsection{Scenario A: T22 Resource-Pool Reallocation}

T22 is removed from every eligible RMG pool and its calendar is closed. The
intervention therefore executes as specified. In the \textit{native-only}
 mechanism configuration, neither mean nor P90 turnaround changes are resolved. Service
and pre-service changes for receive and delivery are likewise unresolved. The
only resolved result is a small decrease in aggregate model-derived RMG
pre-service delay. The workload-blind \textit{visit-only} negative control also
shows no end-to-end response and only a small pre-service change. Removing an
eligible statistical resource is observable, whereas a physical block-closure
effect is not established.

The experiment is not a full block-closure study. The remaining 21 blocks stay
eligible for all RMG activities. Each keeps capacity three. The selector moves
T22 work to another statistical resource. It does not check container
location, block compatibility, relocation, or added travel. The result only
tests the abstract resource pool.

\subsection{Scenario B: 20\,\% Higher Demand}

The working-time intervention is exact: the configured rate rises by 20\,\%.
Measured over elapsed calendar time, the increase is 15.910\,\% because closed
gate periods remain fixed. Timestamps are also floored to minutes. This
flooring turns more short positive intervals into simultaneous observations.
It does not change the zero mass in the input distribution.

The paired intervals for mean turnaround, RMG service, RMG pre-service, and the
receive- and delivery-specific measures all include zero. The
\textit{native-only} model therefore transmits the intervention to its dynamic
state but does not produce a resolved mean handling or end-to-end response at
this demand level.

The result is interpreted together with the response surface below. A 20\,\%
change in working-time intensity produces only a 15.910\,\% increase in
elapsed arrival rate and remains below the fitted model's transition region.
It cannot show that the real terminal could absorb 20\,\% more demand without
congestion. The model still lacks container location, physical movement,
ship-side crane allocation, and an observed queue against which its internal
queue state could be validated.

\subsection{Sensitivity to the Expert-Validated RMG Ceiling}
\label{sec:results_rmg_cap_sensitivity}

The \textit{visit-only} historical-reference model is evaluated both with the
expert-validated ceiling of three RMGs per block and with the overlap-derived
capacities of four or five. Table~\ref{tab:rmg_cap_sensitivity} reports paired
cap-three-minus-uncapped differences on the untouched test period. This is the
matching sensitivity required to distinguish a domain constraint from a
performance-improving calibration.

\begin{table}[htbp]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{L{5.0cm}rr}
\toprule
\textbf{Metric} & \textbf{Cap-three minus uncapped} & \textbf{95\,\% paired MC CI} \\
\midrule
Turnaround EMD (min) & $-0.051$ & [$-0.268$, $+0.167$] \\
Mean turnaround (min) & $+0.135$ & [$-0.063$, $+0.333$] \\
P90 turnaround (min) & $+0.100$ & [$-0.306$, $+0.506$] \\
Weighted yard-service EMD (min) & $-0.024$ & [$-0.079$, $+0.031$] \\
Yard-activity-rate L1 error & $+0.00003$ & [$-0.00217$, $+0.00222$] \\
\bottomrule
\end{tabular}
\caption{Paired RMG-cap sensitivity of the historical \textit{visit-only}
reference model on the test period ($N_{\text{seeds}}=10$).}
\label{tab:rmg_cap_sensitivity}
\end{table}

Every interval here includes zero. The expert ceiling is therefore retained because
it represents a validated physical bound. It does not mean, that it improves historical
fidelity. Within this abstract pooled-resource model, replacing capacities of
four or five with three does not substantially alter the reported baseline. This
null sensitivity also gives an indication, that the resource abstraction does not generate
the congestion response that might be expected from a spatially and resource sensitive
terminal model.

\subsection{Capacity Pressure of the Demand Intervention}

The independently offered-load diagnostic adds an important qualification to
that interpretation. Across the 21 RMG blocks observed in the held-out period,
T13 has the highest nominal utilisation under a ceiling of three concurrent
tasks, with 0.422 at the held-out arrival rate and 0.506 after multiplying arrivals
by 1.20. If block routing and service times remained unchanged, the smallest
arrival multiplier required for any block to reach mean saturation would be
2.37, not 1.20. The initial high utilization of Block T13 is supposedly originating from its central location alongside the Quayside, reducing the container movement when its future carrier position is unknown at time of its arrival at the treminal. Figure~\ref{fig:rmg_capacity_pressure} shows the complete
block-level comparison.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/results_rmg_capacity_pressure.png}
    \caption{Held-out RMG offered load under the three-task block ceiling and
    the mechanical 20\,\% arrival-rate increase. The diagnostic holds routing
    and service times fixed and therefore measures scenario pressure, not
    queueing delay.}
    \label{fig:rmg_capacity_pressure}
\end{figure}

The intervention is therefore not a severe mean-capacity stress test in the
pooled block abstraction. This helps explain why the resource ceiling alone
does not generate a large turnaround response. It does not prove that queues
are absent. Twelve blocks contain at least some held-out minutes with more
than three overlapping recorded RMG events. For T13 the share is 9.82\,\%.
Those bursts may reflect genuine short-period pressure, minute-resolution
artifacts, waterside/landside allocation, or an incorrect block-pool
abstraction. The 20\,\% result alone therefore cannot distinguish an
intervention that is too modest from a model that is unable to propagate
capacity pressure. The paired demand-saturation ladder distinguishes these
possibilities by comparing the workload-blind \textit{visit-only} model with
the \textit{native-only} model under identical demand levels.

\subsection{Demand-Saturation Stress Test}
\label{sec:results_saturation}

The concluding stress test compares the cap-three \textit{visit-only} and
\textit{native-only} Train-80 bundles under exactly the same six demand levels
and seeds. It contains 60 runs per model and 120 runs in total. The configured
multiplier scales working-time inter-arrivals. The realised rate ratio is
measured over elapsed calendar time. These quantities diverge because the gate
calendar remains fixed while the simulated horizon becomes shorter. Every run
preserves the case count, case boundary, and no-overlap contracts.
Table~\ref{tab:demand_saturation_ladder} reports the \textit{native-only}
response. The \textit{visit-only} result is used as the matched
workload-blind negative control.

\begin{table}[htbp]
\centering
\scriptsize
\setlength{\tabcolsep}{2.8pt}
\begin{tabularx}{\textwidth}{@{}rrrrXrX@{}}
\toprule
\textbf{Configured} & \textbf{Realised} & \textbf{Arrivals} & \textbf{Mean} & \textbf{Paired turnaround change} & \textbf{RMG pre-} & \textbf{Paired pre-service change} \\
$\boldsymbol{m}$ & \textbf{rate ratio} & \textbf{per hour} & \textbf{turnaround} & \textbf{[95\,\% paired MC CI]} & \textbf{service} & \textbf{[95\,\% paired MC CI]} \\
\midrule
1.0 & 1.000 & 72.452  & 33.635 & reference & 8.031  & reference \\
1.2 & 1.159 & 83.979  & 33.548 & $-0.087$ [$-0.281$, $+0.106$] & 7.969  & $-0.062$ [$-0.139$, $+0.014$] \\
1.5 & 1.378 & 99.855  & 33.680 & $+0.045$ [$-0.102$, $+0.191$] & 7.958  & $-0.073$ [$-0.116$, $-0.029$] \\
2.0 & 2.367 & 171.472 & 33.718 & $+0.082$ [$-0.101$, $+0.266$] & 8.057  & $+0.026$ [$-0.155$, $+0.207$] \\
2.4 & 2.825 & 204.697 & 35.450 & $+1.815$ [$+0.858$, $+2.772$] & 10.349 & $+2.318$ [$+1.132$, $+3.504$] \\
3.0 & 3.504 & 253.884 & 37.704 & $+4.069$ [$+3.743$, $+4.396$] & 13.251 & $+5.221$ [$+4.878$, $+5.563$] \\
\bottomrule
\end{tabularx}
\caption{Demand-saturation response of the Train-80 \textit{native-only}
 cap-three demand-mechanism configuration ($N_{\mathrm{seeds}}=10$). Times and paired
changes are in minutes. The realised rate ratio uses each level's mean elapsed
arrival rate relative to the matched baseline.}
\label{tab:demand_saturation_ladder}
\end{table}

Up to the realised rate ratio 2.367, the \textit{native-only} mean-turnaround
and mean RMG pre-service intervals include zero. At ratio 2.825, mean
turnaround rises by 1.815 minutes and mean RMG pre-service by 2.318 minutes.
The corresponding mean and P90 intervals are all above zero, and the response
strengthens further at the highest demand level. The first resolved end-to-end
response is therefore bounded between realised rate ratios 2.367 and 2.825.
This interval describes the transition region of the fitted model. It is not
an estimate of a physical CTB capacity threshold.

The negative control shows why historical fidelity alone is insufficient for
intervention analysis.
\textit{Visit-only} remains effectively invariant even at the highest demand
level. The native-minus-visit difference-in-differences at the first resolved
response level, ratio 2.825, is
$+1.851\;[+1.034,+2.669]$ minutes for mean turnaround and
$+2.284\;[+1.120,+3.448]$ minutes for mean RMG pre-service. Both contrasts grow
further at the highest demand level. The response is therefore associated with
access to ProSiT's native dynamic state rather than with the changed arrival
sample alone.

The fitted mechanism is inspectable. The Train-80 \textit{native-only}
waiting-time model retains one dynamic split, for T13 at
\texttt{queue\_length}$\leq6.5$. The lower-queue leaf has a representative
value of 7.167 minutes and an exponential distribution. The higher-queue leaf
has a representative value of 52.404 minutes and a lognormal distribution.
When higher demand makes the second leaf more frequent, pre-service and
turnaround increase non-linearly. This is an automatically discovered
state-conditioned waiting rule. It is not a causal queueing law or proof that
T13 physically changes behavior at exactly 6.5 trucks.

The missing response at 20\,\% is therefore evidence that the intervention is
too modest to enter the response region of this abstract model. The stress
curve is still not a physical CTB forecast. Its onset and shape depend on
interchangeable block assignments, fully open CTB-calibrated resource calendars,
and model-derived enablement rather than verified spatial queues.  In addition,
the fixed-case-count design is a compressed-arrival-horizon experiment: it
changes not only arrival intensity but also where arrivals fall relative to
gate closures, the exposure to end-of-horizon effects, and the calendar-induced
composition of completing cases.  The reported transition region therefore
reflects the combined effect of demand compression and these confounds, not
resource-capacity saturation alone.  The
expert-validated three-RMG ceiling supports the capacity bound. It does not
validate these propagation semantics. Even at the 3.504 realised rate ratio,
mean turnaround remains below the observed hold-out mean of 39.74 minutes.
Artificial overload therefore cannot replace the omitted transition-time and
spatial mechanisms needed for baseline fidelity.

\subsection{Historical-Fidelity Selection and Intervention Adequacy}

The four-cell validation experiment and the intervention tests answer
different questions. Under the pre-specified RQ3 rule, \textit{visit-only} is
the historical-fidelity reference because no added state source produces a
resolved validation improvement. None of the four historical-fidelity
differences between \textit{native-only} and \textit{visit-only} is resolved.
This is not a formal equivalence or non-inferiority result. Small point
differences and intervals containing zero do not establish historical
equivalence.

For the demand intervention, the required pathway is changed arrivals updating
an endogenous state variable that is subsequently available to timing or
resource behaviour. \textit{Visit-only} and \textit{static-only} do not contain
that pathway. \textit{Native-only} is the minimal tested configuration that
does, while \textit{both} adds static historical proxies that do not update
with the intervention. This semantic eligibility defines the mechanism probe.
The later stress response is its outcome, not its selection criterion.

The T22 intervention requires a different mechanism. It tests whether a
resource pool can be removed and reassigned in the executable model. Because
no configuration contains persistent container location, relocation, or
movement, a weak T22 response cannot be interpreted as evidence that closing a
physical block would have little effect. The two interventions therefore probe
different validity boundaries rather than one generic scenario capability.

The mechanism audit was added after the workload-blind stress diagnostic. It is
therefore adaptive and exploratory, not an independently confirmed second
selection procedure. Reporting both stages preserves the evidential
distinction. RQ1 evaluates historical reproduction with the selected
\textit{visit-only} benchmark. RQ4 evaluates intervention mechanisms with the
minimal endogenous-state \textit{native-only} configuration and uses
\textit{visit-only} as a negative control.

\medskip

\noindent\textbf{RQ4 answer.} Both parameter interventions execute as specified.
T22 assignments fall to zero, and working-time arrival intensity rises by
exactly 20\,\%. The workload-blind T22 negative control produces no resolved
end-to-end change and only a small increase in model-derived RMG pre-service
delay. In the \textit{native-only} demand-mechanism configuration, the 20\,\% intervention also
produces no resolved handling or mean-turnaround response. The capacity audit
shows why this is plausible inside the fitted model. The most-loaded held-out
block reaches only 0.506 nominal mean utilisation under the three-task
ceiling.

The paired stress test addresses the narrower functional question of whether
increased arrivals can propagate through the fitted model under progressively
stronger input perturbations. The
\textit{visit-only} model remains invariant even at the highest tested demand,
whereas \textit{native-only} develops a resolved delay response between
realised arrival-rate ratios 2.367 and 2.825. At ratio 3.504, mean turnaround
increases by 4.069 minutes and mean RMG pre-service by 5.221 minutes. The
matched native-minus-visit contrasts confirm that this difference is not
explained by Monte-Carlo variation. The retained T13 queue-length split gives
an inspectable statistical mechanism for the non-linear response.

An internal response is a minimum requirement for using a simulator in a
what-if comparison, but it is not by itself a credible operational prediction.
Candidate truck-slot rates and handling policies can be explored before they
are applied, while the reported transition region remains a property of the
fitted simulation rather than a physical CTB congestion threshold. Spatial
assignment, movement, equipment allocation, and observed queue semantics remain
insufficient for a prescriptive recommendation. Executability and a plausible
stress response do not establish counterfactual validity.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Interpretation: From Process Mining to Data-Aware Simulation}
\label{sec:results_interpretation}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The results become most informative when the structural, temporal and
scenario findings are interpreted together. They do not describe one model
that is simply ``good'' or ``bad.'' Instead, they show that different forms of
process-mining evidence answer different questions. The compact repaired
source net reproduces the test control flow while generalising across sequential
yard combinations, the arrival model reproduces an important part of the
workload timing, and the simulator remains too fast at the level of complete
truck visits. These findings are compatible because control-flow conformance,
arrival fidelity, and end-to-end temporal fidelity are distinct properties
\cite{Camargo2021,ChapelaCampa2023Trust,Chapela2025SimQuality}.

\paragraph{Meaning for traditional Process Mining}
Traditional Process Mining is strongest in this study as a language for
reconstructing and checking observed process behavior. Here, its most
important diagnostic is not merely complete test fitness. Preserving the ITS
schedule-derived stop order removes a timestamp-sorting artefact, but the raw
64\,\% Inductive-Miner tree still contains three parallel operators. The
algorithm interprets reversed sequential relations as concurrency even though
terminal experts state that one truck is serviced sequentially. The
expert-repaired tree
has no parallel operator because this semantic conflict is repaired explicitly,
not because discovery alone resolves it. This demonstrates a classical
Process-Mining point with direct simulation consequences: the event-log
ordering relation and the domain meaning of a discovered operator both require
validation
\cite{PMRozinat2009,PMvanDerAalst2016}.

The expert-repaired control-flow model makes the
fitness--precision--generalisation trade-off visible.
It fits 100\,\% of test traces and retains a test generalisation of
0.9488, including behavior not stored as an exact development path. Precision is
lower at 0.7641 because the loop admits additional yard combinations and
repetitions. In this case, that broader language is not equivalent to physical
parallelism and is not automatically an error: the terminal process allows
varying sequential activity orders. The domain check must therefore be
semantic and operator-specific. Penalising every unseen order would collapse
discovery into memorisation, while accepting a parallel cut would violate the
truck-visit contract.

The silent gate-only path in the raw tree makes the same trade-off concrete.
It is retained in the discovery audit but removed from the executable source
model because experts reject a visit without yard work. Formal language
analysis and realised-output validation still answer complementary questions:
the former identifies what could happen, while the latter confirms that all
reported replications satisfy the repaired contract.

This is a precision-side model-repair problem. The simulation-guided
control-flow repair framework \cite{SimulationGuidedRepair2025} provides a rigorous way to
identify differences between real and simulated control flow and to compare an
original and repaired net on held-out data. Its published operators currently
add missing behavior, however, whereas the CTB bypass requires restricting
excess behavior. A state-aware stochastic Petri-net study
\cite{Vinci2023Inf.Activ.Prob} demonstrates that an explicit manual restriction before simulation can be defensible when a
source model permits a minimal trace absent from the log. For CTB, the
restriction is therefore documented as the discovery-to-execution repair and
applied before every ProSiT configuration is fitted. It is neither hidden nor
selected from validation performance.

The same separation applies to time. Good control-flow coverage does not
account for the 5.930-minute mean turnaround deficit or the 13.100-minute P90
deficit. The simulator can put activities in the correct order and still omit
time between them. In the CTB setting, this missing time may contain movement,
traffic, resource interaction, queueing or operational states that are not
represented as events. The temporal bias is therefore not a contradiction of
the conformance result. It identifies a different abstraction boundary. This
is why evaluating an event-log-derived simulator only through replay fitness
or variant frequencies would have produced an overly positive conclusion.

The transition-baseline diagnostic makes the scale of that abstraction
boundary concrete. Route-specific p5 values lie between 2.16 and 5.28 minutes,
and the frequency-weighted inbound and outbound lower-tail components sum to
7.62 minutes per visit. Their proximity to the 5.930-minute mean deficit is
consistent with omitted physical movement being a major contributor. It does
not prove that all residual time is travel, because the baseline also contains
unseparated traffic and operational delay and because a percentile summary
cannot be subtracted case by case. The fair conclusion is therefore stronger
than merely saying that ``some time is missing,'' but weaker than claiming a
validated travel-time mechanism.

\paragraph{Meaning for data-aware Process Mining}
Recent data-aware methods extend conventional discovery by conditioning
routing, duration or resource behavior on case attributes and process state
\cite{mannhardt2023stochasticProcessesModelling,Vinci2023Inf.Activ.Prob,Vinci2026prosit}.
The CTB results show both the value and the limit of that extension. Its value
is selective rather than universal. Calendar context contains a strong and
stable signal for arrivals: the time-aware benchmark reduces arrival-count
MAE by 67.5\,\% and RMSE by 51.2\,\%, and the day arrival rate is about 8.7
times the night rate. In this part of the process, conditioning on information
available at case creation captures a real predictive regularity.

The duration results point in the other direction. For the two dominant RMG
activities, the diagnostic decision trees have slightly negative test
$R^2$. In the factorial validation experiment, none of the three
additional-state configurations achieves a resolved improvement over
\textit{visit-only} without a resolved loss elsewhere. Static demand,
utilisation and rank variables do appear in learned rules, and the native
models retain one queue-length split. Their interpretability therefore does
not imply improved temporal transfer. This is not proof that workload never
matters. It shows that the available CTB representations do not add validated
predictive value under the tested specification.

This qualification is central to the interpretation of data awareness.
Adding attributes increases representational capacity, but useful capacity
depends on information timing, measurement quality and a relationship that is
stable across the training and application regimes. Case-level target demand
or utilisation may be correlated with an observed duration without measuring
the physical mechanism that caused it. Conversely, a weak marginal
relationship can hide omitted event-level states such as the actual queue,
truck position or equipment movement. The white-box character of ProSiT is
valuable here because the learned conditions can be inspected and challenged.
interpretability makes the association auditable, but does not convert it into
causality or guarantee predictive validity
\cite{Vinci2026prosit,Sargent2013Validation}.

This result sharpens rather than contradicts earlier process-state-abstraction
experiments \cite{Vinci2023Inf.Activ.Prob}. The best-performing abstraction
varies between the two evaluated processes, and an unsuitable history state can
create problematic simulated behavior. The CTB ablation supplies the missing
application-specific test: neither workload source improves the joint
validation criteria sufficiently to replace the parsimonious visit/process
state. It also goes beyond data-aware routing alone. The framework of
López-Pintado et al. \cite{lopez2024} demonstrates the value of case data for
control-flow decisions but identifies data-dependent activity duration as a
separate extension. CTB shows why that separation matters in physical
logistics: state-dependent firing probabilities cannot by themselves create
travel, queues, or spatially constrained service.

The resource result is equally consistent with the differentiated-resource
evidence of López-Pintado and Dumas \cite{BPSdifferentiatedResources2022}.
Treating resources as statistically interchangeable can preserve event counts
while distorting timing. In CTB, capping nominal RMG concurrency does not make
target assignment spatially sticky and does not model crane or truck movement.
By contrast, physical container-terminal simulation represents travel,
equipment interaction and allocation policies explicitly
\cite{Kastner2021}. The current hybrid research opportunity is therefore to
retain the discovered white-box process and state rules while supplying a
mechanistic movement, queue, and resource-assignment layer.

\paragraph{Correlation, scenarios, and operational meaning}
The results distinguish predictive association from intervention response alone. The temporal arrival features display a strong association with when
trucks enter the terminal, but the 20\,\% arrival intervention produces no
resolved mean-turnaround increase. This combination does not imply that real
CTB turnaround is independent of demand. It shows that the fitted simulator
contains an arrival relationship, while the offered-load calculation, places
the chosen change below mean block saturation. The paired ladder adds the
missing functional test. The workload-blind model does not respond even under
extreme demand, while the minimal native-state model develops a strong
pre-service and turnaround response once the realised rate passes the
independently estimated capacity region. The only retained dynamic split in
the Train-80 \textit{native-only} waiting model links T13 queue length to two
different waiting-time
distributions. This provides a direct explanation of the response inside the
simulator without establishing the same threshold as a physical law. In the
same way, eliminating T22
assignments verifies that the resource rule was
changed, while the near-zero turnaround effect reflects reallocation within an
abstract interchangeable resource pool. It is not evidence that closing a
physical block would have no operational effect.

Taken together, the intervention results act as stress tests of the model's
semantics. A null effect can be a substantive operational finding only when
the mechanisms through which the intervention should propagate are represented
and validated. Here, the saturation experiment demonstrates a state-mediated
response under compressed demand only when native dynamic state is enabled.
This separates historical replay fidelity from intervention validity, but it
does not validate the physical route through which that response would arise.
Location feasibility,
movement, differentiated crane availability and spatial queue state remain
absent. The contribution of the
data-aware white-box model is consequently twofold: it supports reproducible
and inspectable changes, and it makes the boundary between an implemented
association and a credible physical mechanism visible. That boundary is the
main result for decision support. The model is suitable for analysing
conditional consequences inside its documented abstraction, but not yet for
claiming causal terminal effects from the scenarios or stress ladder.

The target application nevertheless remains concrete. Before publishing truck
appointments, a terminal could combine a utilisation forecast with candidate
hourly slot limits, simulate each policy, and compare turnaround together with
receive- and delivery-specific service and pre-service KPIs. If forecast
conditions threaten retrieval performance, a candidate policy could reduce
the admitted truck rate or prioritise retrieval-compatible resources. The
simulator would quantify both the intended benefit and any transferred delay
to delivery operations. This prospective comparison is the benefit that a
validated logistics simulation should provide. The CTB experiments establish
the transparent experimental interface and show exactly which additional
queue, location, and equipment states must be represented before that interface
can be used prescriptively.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Layered Validity Synthesis}
\label{sec:results_layered_validity}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Table~\ref{tab:validity_ladder} brings the diagnostics together. It prevents a
strong result in one perspective from being used to imply validity in another.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{3.0cm}L{2.4cm}X}
\toprule
\textbf{Validity layer} & \textbf{CTB assessment} & \textbf{Decisive evidence} \\
\midrule
Event and case semantics & Supported & ITS schedule-derived stop order stored in XES and no case-contract violations in accepted runs. \\
Control-flow language & Supported after repair & Fitness 1.0000, precision 0.7641, no parallel operator, and no gate-only bypass. \\
Historical stochastic fidelity & Mixed & Arrival and yard-service distances are moderate. Mean and tail turnaround remain too short. \\
Temporal transfer & Not stable & Real mean turnaround rises by 2.450 minutes and all yard-service means increase. \\
Resource and spatial semantics & Not supported physically & The cap limits concurrency but does not enforce target location, travel, queue, or equipment movement. \\
Scenario response & Responsive inside native-state model, not physically validated & Policy inputs change reproducibly. A non-linear pre-service and turnaround response appears around the fitted saturation region, while the workload-blind control remains invariant. \\
Prescriptive counterfactual use & Not yet supported & Intervention mechanisms and stakeholder plausibility require further validation. \\
\bottomrule
\end{tabularx}
\caption{Layered validity assessment of the CTB simulation.}
\label{tab:validity_ladder}
\end{table}

The boundary is systematic. Traditional Process Mining supports the first two
layers particularly well: it reveals ordering semantics, generalisation, and
excess language. Data-aware simulation adds auditable conditional routing and
timing rules and supports the third layer where suitable state is available.
The later layers require mechanisms that are not implied by correlation alone.
Recent work is moving in the same direction: evolving-process discovery
addresses drift \cite{vinci2025onlinediscoverysimulationmodels}, and temporal
deviation analysis seeks to attribute simulation mismatch to arrival,
processing, and resource inputs rather than reporting magnitude alone
\cite{Wang2026TemporalDeviations}. The CTB study contributes a physical-
logistics instance in which those layers fail at different points.

This framing also clarifies the role of expert intervention. A hospital
simulation-discovery application combines automatic discovery with manual
structural changes and expert plausibility assessment before optimisation
\cite{Hospital2026Vinci}. The CTB source-model repair follows the same
principle of explicit and auditable domain knowledge. A comparable
expert validation of queue, travel and resource semantics remains necessary
before slot-rate or handling-priority recommendations are presented as terminal
counterfactuals.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Methodological Limitations}
\label{sec:limitations}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\paragraph{Pre-Service Delay}

ProSiT enables an activity when the process model allows it. Consecutive CTB
activities can occur at different locations, but truck movement is not a
separate activity in the event-log design. The time from completion of one
activity to the start of the next can therefore combine
\begin{equation}
    D_{\mathrm{pre\text{-}service}} \approx
    T_{\mathrm{physical}} +
    T_{\mathrm{traffic}} +
    T_{\mathrm{queue}} +
    T_{\mathrm{other}}.
\end{equation}
The presented event log cannot identify these parts separately. The percentile check shows
that a reconstructed baseline is sensitive to its assumption. This would require position, trajectory, queue-entry, or
similar state data. These data are not available at the execution of this thesis. The results use
the intended term \textit{pre-service delay}, not queueing time.
The model's enabled timestamp is structurally auditable because it follows the
Petri-net marking. Its accuracy as a physical readiness timestamp is not
auditable without independent readiness observation data. The activity-specific
composite-gap comparison therefore tests the temporal consequence of the
enablement convention, not the latent timestamp itself.

\paragraph{Service-Time Semantics}

The interval from \texttt{BEREITMELDUNG} (reported ready at the handover
location) to \texttt{ERFUELLUNG} (fulfilled) is the period during which the
truck driver is present at the handling point and dependent on terminal
operations, like crane positioning, container retrieval or placement including
restacking, and completion confirmation. Physical queueing and transit from
the previous location occur \emph{before} the ready timestamp and are
therefore part of the model-derived pre-service delay, not the service time.
The label \emph{service time} is consequently used in its standard BPS meaning.
Longer values reflect heavier terminal handling requirements, not driver-side
waiting.  The offered-load calculations use this definition. Their validity
depends on the ready timestamp accurately marking the start of terminal
service rather than an earlier dispatching event.

\paragraph{Resource Assignment Is Not Spatial Container Relocation}

An RMG resource in ProSiT is an abstract capacity assignment. The discovered
overlap capacity is 100. The stated physical cap is 66 across 22 blocks. This
mismatch shows that timestamp overlap is not a crane count. The selected
scenario reference model uses the cap of three per block.

The cap does not add spatial state. Target-area attributes are predictors, not
hard inventory constraints. The simulator does not bind a container to a
storage block. It also does not create relocation work after reassignment.
Closing T22 spreads its statistical demand across the remaining blocks. It
does not model feasibility, crane movement, or added truck travel. Scenario~A
tests the resource-pool abstraction. It does not forecast a physical T22
closure.

\paragraph{Initial State at the Temporal Cutoff}

Each replication starts from the Petri-net initial marking with no active
truck cases and empty resource schedules. Trucks that entered before the test
cutoff but remained inside the real terminal are not reconstructed. The
historical comparison is therefore conditional on an empty simulated system
at the beginning of the period. This can bias the earliest generated cases and
is one reason not to interpret the test as a live digital-twin initialization.
All matched scenario alternatives share exactly the same empty state, so this
boundary does not by itself create their paired differences. Operational use
would require work-in-progress initialization or a documented warm-up design.

\paragraph{Scope of Expert Validation}

The project-level expert review confirmed the gate boundaries, the requirement
for at least one yard activity, arbitrary yard ordering and repetition, the
validity of \texttt{LL\_mixed}-only yard visits and the invalidity of gate-only
visits. It also confirmed two hard physical constraints. One truck does not
execute activities in parallel, and no block can use more than three RMGs
concurrently. These judgments support the case contract, operator check,
bypass diagnosis and cap-three correction. They do not validate the discovered
HO2 or LL capacities, the fully open CTB-calibrated resource calendars, arrival and duration
distributions, block-interchangeability assumption, pre-service-delay
semantics, or any scenario response. The review was a requirements check and
did not produce a formal transcript or inter-rater assessment. Its evidential
scope is therefore narrower than a structured expert-validation study.

\paragraph{The Empirical Population Excludes Multi-RMG Visits}

The case-level ProSiT representation requires one unambiguous RMG target for
the block-specific context variables. The pipeline therefore removes 1,693
visits and 6,820 events involving more than one RMG block. The results apply to
the resulting one-RMG subprocess, not to the complete population of CTB truck
movements. The exclusion removes some of the visits most likely to contain
additional movement, cross-block resource interaction and spatial complexity.
It may therefore make transport and congestion behavior easier than in the
full terminal population. An object-centric representation could preserve the
truck, containers and multiple blocks as related objects instead of forcing one
block value onto the complete visit
\cite{VanDerAalst2023OCPM,Knopp2023ObjectCentricSimulation}. This is a direct
empirical scope boundary, not only a general future-work opportunity.

\paragraph{Scenario Control Flow Generalises Only Within a Fixed Activity Set}

The Inductive-Miner loop can generate sequential combinations and repetitions
that were not present as complete training variants. This is less restrictive
than the earlier trie representation, but it is not an open-ended terminal
model. A scenario cannot create a new activity type, an unrecorded exception
mechanism, or a structurally different response to disruption. The two policy
experiments and the demand ladder therefore test parameter changes inside the
discovered activity language. Missing structural mechanisms may still remove
real sources of delay.

\paragraph{Systematic Turnaround Under-Prediction}

The data-aware reference model under-predicts mean turnaround by 5.930 minutes and P90 by
13.100 minutes. The narrow seed intervals show that this discrepancy is stable
for the fixed model and test period rather than an artefact of ordinary
run-to-run simulation noise. They do not identify its cause. The log cannot
show how much missing time comes from
movement, traffic, queueing, or an unrecorded state. Correct case order does
not remove this limit on KPI forecasts.

\paragraph{Scenario Intervals Cover Only Part of Structural Uncertainty}

The matched-seed intervals in Tables~\ref{tab:scenario_kpis},
\ref{tab:demand_scenario_kpis}, and
\ref{tab:demand_saturation_ladder} cover random
variation in one fixed model. They do not cover a new training period, another
discovery run, or missing spatial state. The cap sets a ceiling but adds no
location feasibility or equipment movement. More seeds would narrow these
intervals. They would not reduce structural uncertainty. The matched
state-configuration comparison addresses one part of that uncertainty: it
shows that high-load response depends on whether native dynamic state is
available. It still does not cover a new training period, another discovered net,
alternative resource semantics, or the missing spatial and queueing state.

\paragraph{External Validity Is Limited to One Terminal and Time Window}

The temporal hold-out is one later period from the same terminal and source
systems. Near-perfect replay fitness shows structural stability in this
period, but the observed 2.450-minute mean-turnaround shift shows that temporal
stationarity does not hold even within the study window. The results do not
cover another terminal, season, gate regime, or infrastructure setup. That
would require another full discovery and validation run. An adaptive or
rolling-window simulator would additionally require prospective evaluation.

\paragraph{Marginal Arrival Validation Does Not Establish Process-Level
Fidelity}

The inter-arrival EMD of 0.072 minutes compares marginal gap distributions.
It does not assess hour-of-day arrival profiles, weekday effects, burst-size
distributions, temporal autocorrelation, or conditional dependencies between
arrival time and visit characteristics.  A simulator can reproduce the
marginal inter-arrival distribution while misplacing arrivals within the day
or suppressing realistic arrival clustering.  These dimensions are not
validated in the current study and should be assessed before the arrival model
is used for time-of-day-sensitive policy comparisons.

\paragraph{Event-Linkage Reliability}

The cross-system identifier match uses plate number and minute-resolved
timestamp.  This composite key works on the evaluated data, but it remains
vulnerable to duplicate plates at the same minute, delayed or retroactive
event recording, and rounding artefacts in other extraction periods.  The
findings therefore demonstrate validity for the evaluated data-generation
process, not unconditional reliability of the linkage procedure across
arbitrary terminal periods or source-system versions.
