\chapter{Results}
\label{chap:results}

This chapter reports the results for the four research questions from
Chapter~\ref{chp:experiments_and_framework}. RQ2 comes first. The control-flow
contract must be usable before timing can be evaluated. RQ1 then compares the
data-aware reference configuration with the temporal hold-out. RQ3 compares
four ProSiT configurations in the standardized $2\times2$ feature-source
design. The next sections examine temporal transfer, context, arrivals,
resources, and transition times. These diagnostics help interpret the results, but they do
not prove their causes. RQ4 evaluates two policy interventions and a
demand-saturation response surface.

The chapter uses six types of evidence: observed train--test transfer,
real-versus-simulated hold-out comparisons, paired configuration contrasts,
a structural robustness repair, paired policy changes, and a saturation stress
test.
They answer different questions. The limitations are discussed before the
final answers to RQ1--RQ4.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Temporal Train-Test Split}
\label{sec:results_eventlog_char}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The temporal split contains 89,460 cases. The first 71,568 cases form the
training log. They contain 220,048 events and cover Gate In arrivals from
\text{2026-03-02 05:27} to \text{2026-04-20 18:15}. The next arrival at
\text{2026-04-20 18:17} starts the test log. It contains 17,892 cases and
55,360 events. No event is removed because of a missing timestamp.
Table~\ref{tab:activity_counts_split} gives the activity counts.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Activity} & \textbf{Train} & \textbf{Test} & \textbf{Total} \\
\midrule
Gate In       & 71,568 & 17,892 & 89,460 \\
Gate Out      & 71,568 & 17,892 & 89,460 \\
RMG\_receive  & 32,794 & 7,171  & 39,965 \\
RMG\_delivery & 17,823 & 4,681  & 22,504 \\
RMG\_mixed    & 3,986  & 953    & 4,939  \\
LL\_delivery  & 9,612  & 3,618  & 13,230 \\
HO2\_delivery & 5,775  & 1,449  & 7,224  \\
HO2\_receive  & 4,188  & 1,276  & 5,464  \\
HO2\_mixed    & 749    & 276    & 1,025  \\
LL\_receive   & 1,932  & 141    & 2,073  \\
LL\_mixed     & 53     & 11     & 64     \\
\bottomrule
\end{tabular}
\caption{Activity counts on the train and test partitions.}
\label{tab:activity_counts_split}
\end{table}

All activities also occur in the test log, but their frequencies change.
\texttt{LL\_delivery} rises from 0.134 to 0.202 events per case.
\texttt{RMG\_receive} falls from 0.458 to 0.401, and
\texttt{LL\_receive} falls from 0.027 to 0.008. This shift is kept in the
evaluation. It contributes to the final yard-activity-rate L1 error in
Table~\ref{tab:final_kpis}. A random split would mix this later-period shift
into both partitions. The temporal split does not. It still covers only one
later CTB period.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Control-Flow Stability Across the Held-Out Period}
\label{sec:results_process_discovery}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The final simulator uses the Inductive-Miner Petri net from
Section~\ref{sec:process_discovery_methodology}. The XES log preserves the ITS
schedule-derived stop order. Using that order produces a compact model with
7 places, 16 transitions, 32 arcs, and no
parallel operator. It abstracts from 70 training variants rather than storing
each path separately. Table~\ref{tab:conformance_train_test} reports its
training and held-out quality measures.

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Metric} & \textbf{Train} & \textbf{Test} \\
\midrule
Log fitness                      & 1.000000 & 0.999992 \\
Fitting traces (\%)              & 100.000 & 99.994 \\
Average trace fitness            & 1.000000 & 0.999993 \\
Token-based precision            & 0.7843 & 0.7558 \\
Generalisation                   & 0.9723 & 0.9413 \\
Simplicity                       & 0.5610 & 0.5610 \\
\bottomrule
\end{tabular}
\caption{Inductive-Miner conformance and structural descriptors on train and test.}
\label{tab:conformance_train_test}
\end{table}

The test log contains 53 variants, ten of which are absent from training and
together account for ten of 17,892 cases (0.0559\,\%). Unlike a prefix trie,
the Inductive-Miner model can replay unseen sequential combinations. It fits
17,891 held-out cases completely and attains a log fitness of 0.999992. The
near-perfect score is therefore substantive held-out evidence rather than a
consequence of memorising the construction log.

The remaining measures show the expected abstraction trade-off. Held-out
precision is 0.7558 because the loop permits more sequential combinations and
repetitions than were observed. This broader language is acceptable for the
stated CTB contract: internal yard order is not prescribed, while concurrency
within one visit is prohibited. Generalisation remains high at 0.9413, and the
7-place net is small enough to inspect directly. A hard operator check confirms
zero parallel cuts, and ten context-aware simulation seeds produce neither
within-case overlap nor a \texttt{Gate Out} before the final yard activity. Although the
formal net contains a silent yard bypass, none of the 178,920 simulated cases
from this endpoint uses it to create a gate-only visit. The distribution-only
endpoint is examined separately under RQ3.

\medskip

\noindent\textbf{RQ2 answer.} The automatically discovered Inductive-Miner net
is suitable as the simulation backbone once the ITS schedule-derived stop
order stored in XES is preserved. It combines near-perfect held-out fitness with sequential
generalisation and passes the terminal-expert-validated no-concurrency
contract. Its lower
precision primarily reflects broader yard combinations rather than parallel
execution, but the gate-only bypass is a genuine precision defect.

\subsection{Precision-Oriented Structural Robustness Model}

The separate repair experiment removes that bypass without prescribing the
order or number of yard activities. It adds one place and one silent transition
to distinguish a visit that has already executed \texttt{LL\_mixed} from a
visit that has not yet performed yard work. Table~\ref{tab:gate_bypass_repair}
compares the unchanged discovery result with this repaired robustness model on
the held-out log.

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Held-out measure} & \textbf{Discovered net} & \textbf{Repaired net} \\
\midrule
Log fitness          & 0.999992 & 0.999992 \\
Fitting traces (\%)   & 99.994   & 99.994 \\
Precision            & 0.7558   & 0.7939 \\
Generalisation       & 0.9413   & 0.8859 \\
Simplicity           & 0.5610   & 0.5814 \\
Places / transitions / arcs & 7 / 16 / 32 & 8 / 17 / 34 \\
\bottomrule
\end{tabular}
\caption{Held-out conformance and structure before and after restricting the
silent gate-only bypass.}
\label{tab:gate_bypass_repair}
\end{table}

The repair raises precision by 0.0381 without losing held-out replay fitness
and slightly raises the simplicity score. It also lowers the generalisation
score by 0.0554. This is the expected fitness--precision--generalisation
trade-off made empirical: restricting unsupported language improves precision,
but it reduces the model's measured ability to generalise. The unchanged net
remains the primary automatic-discovery baseline, while the repaired net is
exported as a separate PNML and ProSiT bundle.

The full stochastic robustness comparison finds no realised fidelity cost.
With the final cap-three \textit{visit-only} parameters, all metric cells are
identical between the original and repaired bundles for seeds 42--51. This
includes turnaround and service-time EMD, activity-rate error, all case-contract
counts, and every activity-specific service distance. Completion-order 3-gram
NGD is 0.05649 for both models, and explicit-order NGD is 0.05646 for both. The
paired difference is exactly zero for every seed and ordering definition. The
removed bypass was never realised under these parameters, so the repair
improves the formal process language without changing the sampled baseline
behaviour. This does not show that the branch would remain irrelevant under
every alternative parameterisation or intervention. The scenario conclusions
therefore continue to refer to the unchanged automatic-discovery baseline.

\paragraph{Interpretation of the \texttt{LL\_mixed} Branch}

The special structural role of \texttt{LL\_mixed} is supported by a small and
highly concentrated set of visits. Across the complete train and hold-out
periods, 64 visits contain the activity and 57 of them contain no other yard
activity. Their observed pattern is therefore simply \texttt{Gate In},
\texttt{LL\_mixed}, and \texttt{Gate Out}. Only seven visits combine
\texttt{LL\_mixed} with another yard activity. In six training visits it occurs
first and is followed by \texttt{RMG\_receive}. The single held-out exception
follows the order \texttt{RMG\_delivery}, \texttt{LL\_mixed}. One plausible
operational interpretation is that an empty-container exchange is normally
completed before another yard area for routing reasons. The six training cases
are compatible with that explanation, but the small sample and held-out
exception do not justify a mandatory ordering rule. The isolated branch is
primarily evidence of a valid one-stop process variant rather than evidence
that \texttt{LL\_mixed} must always precede other yard work. The counts and
sequences are exported by
\texttt{validation/20\_ll\_mixed\_sequence\_bottleneck\_audit.py}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Historical Fidelity and Model Screening}
\label{sec:results_holdout_fidelity}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

This section contains two evaluations. RQ1 compares the final cap-three
\textit{visit-only} within-study reference with the temporal hold-out. RQ3 uses
the standardized $2\times2$ feature-source experiment and the clean-static
sensitivity to justify that reference-model decision.
All configurations use the same discovered Inductive-Miner net, temporal split,
arrival calibration, and ten-seed protocol. Their difference is therefore the
way routing, duration, and resource behaviour use contextual information.

\subsection{RQ1: Historical Fidelity of the Data-Aware Reference Model}

The data-aware reference model uses the discovered Inductive-Miner net, native ProSiT
routing learned from training, the minute-resolved arrival model, and
non-pre-emptive completion. The raw RMG
capacities are capped at three per block. This cap comes from the domain, not
from the hold-out. The hold-out is used only for evaluation.

Ten runs of 17,892 cases use seeds 42--51. Each run starts at the frozen cutoff
$t_{\text{start}}=\text{2026-04-20 18:17}$. Table~\ref{tab:final_kpis}
summarises the comparison. Its intervals cover seed variation for this fixed
model and hold-out only.

\begin{table}[htbp]
\centering
\setlength{\tabcolsep}{3.4pt}
\begin{tabular}{L{4.6cm}rrrr}
\toprule
\textbf{KPI} & \textbf{Real/ref.} & \textbf{Simulated} &
\textbf{Difference} & \textbf{95\,\% MC CI} \\
\midrule
Yard service EMD, unweighted (min) & -- & 3.195 & -- & [3.099, 3.291] \\
Yard service EMD, real-frequency weighted (min) & -- & 2.357 & -- & [2.289, 2.425] \\
Case-turnaround EMD (min) & -- & 7.767 & -- & [7.612, 7.922] \\
Case-turnaround KS & -- & 0.1254 & -- & [0.1194, 0.1313] \\
Inter-arrival EMD (min) & -- & 0.0724 & -- & [0.0674, 0.0774] \\
Inter-arrival KS & -- & 0.0254 & -- & [0.0215, 0.0292] \\
Mean turnaround (min) & 39.742 & 32.114 & $-7.629$ & [31.929, 32.298]$^{a}$ \\
P90 turnaround (min) & 71.000 & 54.200 & $-16.800$ & [53.748, 54.652]$^{a}$ \\
Simultaneous-arrival share (\%) & 56.660 & 54.221 & $-2.439$ pp & [54.035, 54.407]$^{a}$ \\
Yard-activity-rate L1 error & -- & 0.1634 & -- & [0.1579, 0.1688] \\
\bottomrule
\end{tabular}
\caption{Held-out fidelity of the calibrated data-aware cap-three reference model
($N_{\text{seeds}}=10$, seeds 42--51; 17,892 cases per run).}
\label{tab:final_kpis}
\smallskip
\parbox{0.95\textwidth}{\footnotesize $^{a}$The interval refers to the
simulated outcome. The real hold-out statistic is fixed. Gate In and Gate Out
are excluded from both yard-service EMD aggregates.}
\end{table}

The result is mixed. Inter-arrival EMD is 0.0724 minutes. Unweighted yard
service EMD is 3.195 minutes. Frequency weighting reduces it to 2.357 minutes
because some of the larger errors occur in rare activities
(Table~\ref{tab:yard_activity_emd}). The complete visit is less accurate. Mean
turnaround is 7.629 minutes too short, or 19.20\,\%. P90 is 16.800 minutes too
short, or 23.66\,\%. This bias remains across the ten seeds. The narrow seed
intervals do not show that the model itself is correct.

All ten runs pass the case-order checks. They contain no gate-only cases,
within-case overlaps, invalid boundaries, or decreasing completion times. Gate
Out never occurs before the last yard activity. No accepted duration exceeds
24 hours. These checks are necessary, but they do not remove the timing bias.

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Yard activity} & \textbf{Mean EMD (min)} & \textbf{95\,\% MC CI} \\
\midrule
HO2\_delivery & 2.196 & [2.040, 2.352] \\
HO2\_mixed    & 5.190 & [4.359, 6.021] \\
HO2\_receive  & 1.974 & [1.761, 2.188] \\
LL\_delivery  & 1.445 & [1.363, 1.528] \\
LL\_mixed     & 4.511 & [4.146, 4.876] \\
LL\_receive   & 4.313 & [3.981, 4.645] \\
RMG\_delivery & 2.504 & [2.411, 2.596] \\
RMG\_mixed    & 4.197 & [4.036, 4.358] \\
RMG\_receive  & 2.426 & [2.320, 2.532] \\
\bottomrule
\end{tabular}
\caption{Held-out service-time EMD by yard activity for the final
cap-three baseline ($N_{\text{seeds}}=10$).}
\label{tab:yard_activity_emd}
\end{table}

\noindent\textbf{RQ1 answer.} The model reproduces the case order and parts of
the arrival and service-time behaviour. It does not reproduce the full
turnaround distribution. Total visit time is systematically too short.

\subsection{RQ3: Feature Sources and Held-Out Fidelity}

Table~\ref{tab:configuration_screening} reports the standardized $2\times2$
experiment. All four cells use the same event-log source, Inductive-Miner net,
split, tree settings, deterministic runner and seeds. They differ only in
whether manually constructed static demand/utilisation proxies and ProSiT's
native workload/queue state are available. The N-Gram Distribution Distance
(NGD), here with $n=3$, compares local activity-order frequencies; lower values are
better for every reported error measure.

\begin{table}[htbp]
\centering
\small
\setlength{\tabcolsep}{3.0pt}
\begin{tabular}{L{4.2cm}rrrr}
\toprule
\textbf{Metric} & \textbf{visit-only} & \textbf{static-only} &
\textbf{native-only} & \textbf{both} \\
\midrule
Turnaround EMD (min) & 7.870 & 7.848 & 7.967 & 8.158 \\
Yard-service EMD, weighted (min) & 2.379 & 2.355 & 2.351 & 2.368 \\
Yard-activity-rate L1 error & 0.1632 & 0.2265 & 0.1635 & 0.2276 \\
3-gram NGD & 0.05661 & 0.07308 & 0.05659 & 0.07349 \\
Inter-arrival EMD (min) & 0.07241 & 0.07241 & 0.07241 & 0.07241 \\
Gate-only cases per run & 0 & 0 & 0 & 0 \\
\bottomrule
\end{tabular}
\caption{Held-out $2\times2$ feature-source comparison
($N_{\text{seeds}}=10$; 17,892 cases per run).}
\label{tab:configuration_screening}
\end{table}

The completion-time ordering used for the primary NGD calculation is not
decisive for this comparison. With explicit \texttt{case:event:order}, mean NGD
is 0.05657 for \textit{visit-only}, 0.07304 for \textit{static-only}, 0.05656
for \textit{native-only} and 0.07345 for \textit{both}. Each value differs by
about 0.00003 from its completion-order counterpart in
Table~\ref{tab:configuration_screening}. The same two practically tied models
remain best, while both configurations containing static proxies remain
materially worse. The reference-model decision is therefore unchanged by the
announced ordering sensitivity.

Static proxies have no resolved turnaround or weighted-service effect when
native state is off: the paired EMD changes are $-0.021$
[$-0.137,+0.095$] and $-0.024$ [$-0.081,+0.033$] minutes. They do, however,
worsen activity-rate error by $+0.0633$ [$+0.0570,+0.0696$] and 3-gram NGD by
$+0.01647$ [$+0.01460,+0.01835$]. The same losses occur when native state is
on. The static proxies therefore change the generated control-flow mixture,
not merely a duration model.

Native workload state has almost no effect on activity-rate error or NGD when
static proxies are off, but raises turnaround EMD by $+0.097$
[$+0.007,+0.187$] minutes. With static proxies present, the increase is
$+0.310$ [$+0.189,+0.431$] minutes. The turnaround interaction is $+0.213$
[$+0.064,+0.361$] minutes: the native-state penalty is larger when static
proxies are simultaneously available. No corresponding weighted yard-service,
activity-rate, or NGD interaction is resolved. This identifies bundle-level
predictive interaction; it does not establish a causal operational interaction
between terminal workload variables.

The rule audit explains why ordinary feature importance would be misleading.
The static-only and both bundles each retain just one split from the static
workload family, on \texttt{target\_rank\_group}; no continuous demand or
utilisation split survives. Native-only and both each retain one
\texttt{queue\_length} split. Most retained rules use visit/process or location
state. Correlation is concentrated within alternative static representations:
$\rho=0.922$ for gate and RMG utilisation, $0.898$ for gate and RMG demand, and
$-0.790$ for target utilisation and target rank. By contrast, the largest
absolute demand--native-state correlation is 0.190 and the largest
utilisation--native-state correlation is 0.019. Correlated attributes may
remain in an event log. The methodological issue is that attribution to an
individual split can depend on the candidate set, not that predictors must be
mutually independent.

The retained native rule also makes the white-box scope concrete. In the
\textit{native-only} and \textit{both} bundles, the waiting-time tree for T13
splits once at \texttt{queue\_length}$=6.5$. Values at or below the threshold
use a fitted exponential distribution with a reported node value of 7.167
minutes. Values above the threshold use a lognormal distribution with a
reported node value of 52.404 minutes. This condition is inspectable and can be
challenged against domain knowledge. It is not treated as a causal queueing law,
and neither operational scenario edits this split or replaces one of its
subtrees. The scenarios demonstrate transparent parameter intervention, while
this retained rule illustrates the additional tree structure that ProSiT makes
available for future rule-level interventions.

The semantically cleaned static sensitivity confirms this boundary. Relative
to visit-only, it changes turnaround EMD by $+0.049$
[$-0.023,+0.121$] and weighted yard-service EMD by $-0.024$
[$-0.095,+0.046$], both unresolved. Activity-rate error rises by $+0.0622$
[$+0.0560,+0.0683$] and 3-gram NGD by $+0.01636$
[$+0.01458,+0.01813$]. None of its retained splits uses demand or utilisation;
the expanded candidate set instead changes which visit and location rules are
selected. The result is evidence of feature-candidate-set or specification
sensitivity. It is not a repeated-sampling demonstration of statistical
discovery instability, and it is not evidence that a retained demand split
directly causes the error. It also removes the rationale for further ad-hoc
feature subsets.

\paragraph{Reference-model decision}

\textit{Visit-only} is fixed as the within-study reference because it is the
most parsimonious cell, has the lowest activity-rate error, and is practically
tied for the lowest 3-gram NGD. Static-only's 0.021-minute turnaround-EMD
advantage is unresolved and accompanied by materially worse generated activity
frequencies and local sequences. The \textit{both} model is therefore recorded
as investigated but rejected. It remains useful only for checking whether the
scenario conclusion depends on this model choice. Because the same temporal
hold-out informed the decision, ``within-study reference'' does not mean an
independently selected prospective model.

\noindent\textbf{RQ3 answer.} Neither static workload proxies nor ProSiT's
native dynamic state improve the multi-perspective historical fit. Static
proxies harm control-flow-frequency fidelity; native state harms turnaround
fidelity, especially when combined with them. More available state is therefore
not automatically more valid process state, and the evidence favours the
parsimonious visit-only model for this event log.

\paragraph{Meaning of the Time Measures}
\label{sec:results_temporal_note}

The real log has start and completion timestamps, but no enablement timestamp.
Service-time EMD uses $\text{complete}-\text{start}$. Turnaround EMD uses
$\text{last\_complete}-\text{first\_start}$. Both can be calculated for both
logs. ProSiT derives enablement internally and writes it only to the simulated
log. Simulated $\text{start}-\text{enabled}$ is called pre-service delay. It
cannot be validated against real waiting time.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Temporal Transfer and Error Decomposition}
\label{sec:results_temporal_transfer}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The observed process changes between discovery and evaluation. Mean truck
turnaround rises from 37.293 minutes in training to 39.742 minutes in the
hold-out, a 2.450-minute increase (95\,\% Welch CI $[2.003,2.896]$).
The median rises from 29 to 32 minutes and P90 from 67 to 71 minutes; the
bootstrapped 95\,\% interval for the P90 increase is $[3,6]$ minutes. The
Wasserstein distance between the two real turnaround distributions is 3.235
minutes. The hold-out is therefore not an exchangeable sample from an
unchanged period.

The change is not confined to case composition. Mean service time increases
for all nine yard activities, with activity-specific increases between 1.20
and 4.01 minutes. Using held-out activity frequencies, their mean shift is
2.120 minutes and their aggregate Wasserstein distance is 2.131 minutes.
At the same time, process-flow composition changes by a total-variation
distance of 0.072 and target-area composition by 0.102. The receive share
falls by 7.22 percentage points.

This drift also challenges a simple congestion interpretation of the available
state variables. Gate and RMG utilisation proxies are lower in the hold-out
(standardised shifts $-1.095$ and $-0.675$), while all yard-service means and
complete turnaround are higher. Several demand measures rise, but the
direction of the utilisation proxies is inconsistent with treating them as a
stable causal measurement of terminal congestion. They remain predictive
features whose relationships must transfer empirically, not mechanistic queue
state. This finding is consistent with recent work on online simulation
discovery, which gives greater weight to recent data while retaining historical
information to handle evolving processes
\cite{vinci2025onlinediscoverysimulationmodels}.

Equation~\ref{eq:mean_transfer_decomposition} separates the final mean deficit
without pretending that the two sources can be identified case by case. For
the final \textit{visit-only} cap-three baseline,
\begin{equation}
39.742-32.114=(37.293-32.114)+(39.742-37.293)
=5.179+2.450=7.629\ \text{minutes}.
\end{equation}
Approximately 68\,\% of the mean deficit is therefore already present relative
to the training-period mean; the remaining 32\,\% is the later-period mean
shift.
This decomposition applies to the means only and is not an additive
decomposition of EMD.

The service-time comparison points in the same direction. An earlier
training-period diagnostic for the workload-blind rules bundle produced a
frequency-weighted yard-service EMD of 0.595 minutes, whereas the standardized
\textit{visit-only} reference reaches 2.357 minutes against the hold-out. The
comparison is descriptive because the bundle versions are not identical, but
it is consistent with substantial temporal transfer in activity timing.
Complete turnaround still has a substantial training-period deficit, which
indicates an additional model boundary beyond drift. The distinction is
important because a direct future-log comparison alone can conflate simulator
error with changing process behaviour \cite{Ozdemir2025UtilityBPS}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Context Signal in Activity Durations}
\label{sec:results_per_activity}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The RQ3 ablation shows that adding the available workload state degrades several
held-out measures without improving service-time EMD. The next diagnostic
checks how much duration information is present in the
available attributes. Table~\ref{tab:tree_r2_train_test} reports a
training-only decision-tree test. It is not a breakdown of the final EMD. It
also cannot prove why the RQ3 result occurs.

\begin{table}[htbp]
\centering
\begin{tabular}{lr}
\toprule
\textbf{Activity} & \textbf{$R^{2}$} \\
\midrule
LL\_delivery  & 0.263 \\
LL\_mixed     & 0.258 \\
HO2\_receive  & 0.115 \\
HO2\_mixed    & 0.070 \\
RMG\_mixed    & 0.051 \\
HO2\_delivery & 0.029 \\
LL\_receive   & 0.024 \\
RMG\_receive  & $-0.005$ \\
RMG\_delivery & $-0.004$ \\
Gate In       & 1.000 \\
Gate Out      & 1.000 \\
\bottomrule
\end{tabular}
\caption{Per-activity duration $R^{2}$ of a decision-tree model
conditioned on target demand, target utilisation, target rank, visit
complexity, receive/delivery counts, hour, weekday and weekend
indicator (training log; grid search over depth/leaf, best per
activity).}
\label{tab:tree_r2_train_test}
\end{table}

Gate activities have $R^{2}=1.000$ because their service duration is fixed at
zero. This is not useful predictive performance. The nine yard activities
range from $-0.005$ for \texttt{RMG\_receive} to $0.263$ for
\texttt{LL\_delivery}. The two main RMG activities are close to zero. RMG
activities still account for more than 60\,\% of yard events.

The available attributes explain little of the duration variation in the
largest activity family. This is consistent with the weak RQ3 result. It is
not proof of its cause. Leaf size, fitted distributions, routing, resource
selection, and the change between train and test may also affect the result.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Arrival Structure as a Counterexample}
\label{sec:results_arrival_process}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The weak duration result does not mean that context is useless everywhere.
Arrivals provide a counterexample. Table~\ref{tab:arrival_benchmark} compares
one pooled arrival rate with a tree using hour, weekday, and weekend features.
The test uses a chronological 80/20 split inside the training partition. It is
a training diagnostic, not another held-out validation result.

\begin{table}[htbp]
\centering
\begin{tabular}{lrrr}
\toprule
\textbf{Model} & \textbf{MAE} & \textbf{RMSE} & \textbf{$R^{2}$} \\
\midrule
Pooled arrival rate      & 28.98 & 34.60 & $-0.001$ \\
Time-aware decision tree & \textbf{9.41} & \textbf{16.87} & \textbf{0.762} \\
\bottomrule
\end{tabular}
\caption{Arrival-count benchmark on 30-minute buckets (training log only,
chronological 80/20 split).}
\label{tab:arrival_benchmark}
\end{table}

The time-aware tree reduces MAE by 67.5\,\% and RMSE by 51.2\,\%. The main
pattern is also visible in Table~\ref{tab:day_night}. The estimated day rate is
about 8.7 times the night rate. Arrival intensity is not constant.
Time conditioning is useful here. The result does not show that the tree
captures every source of arrival variation.

\begin{table}[htbp]
\centering
\begin{tabular}{lrr}
\toprule
\textbf{Period} & \textbf{Mean inter-arrival (min)} &
\textbf{Implied mean arrivals/hour} \\
\midrule
All   & 1.00 & 60.2 \\
Day   & 0.54 & 110.9 \\
Night & 4.69 & 12.8 \\
\bottomrule
\end{tabular}
\caption{Day and night arrival statistics on the training log.}
\label{tab:day_night}
\end{table}

The low inter-arrival EMD in Table~\ref{tab:final_kpis} comes from the final
calibrated simulator, not from this tree alone. More importantly, good arrival
timing does not fix the turnaround bias. The missing time occurs after arrival.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Resource Discovery and the Domain Cap}
\label{sec:results_resource_capacities}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The discovered source bundle contains 26 resource identifiers.
Table~\ref{tab:capacities_train80} shows their maximum concurrency. The source
bundle is kept for audit. The final runs use a copy in which each RMG block is
capped at three, the ceiling confirmed by terminal experts. All other
parameters stay unchanged. The earlier pool-level
report from \texttt{baseline/04\_resource\_concurrency\_discovery.py} is only a
diagnostic. It is not the simulator configuration.

\begin{table}[htbp]
\centering
\begin{tabular}{L{1.3cm}L{3.1cm}L{4.0cm}L{2.6cm}}
\toprule
\textbf{Family} & \textbf{Identifiers} & \textbf{Eligible activities} &
\textbf{Maximum concurrency} \\
\midrule
Gate In  & Res.GateIn  & Gate In & 1 \\
Gate Out & Res.GateOut & Gate Out & 1 \\
HO2      & HO2 & three HO2 activities & 15 \\
LL       & LL  & three LL activities  & 22 \\
RMG      & T06--T27 (22 blocks) & three RMG activities & 4--5$^{a}$ \\
\bottomrule
\end{tabular}
\caption{Resource representation in the freely discovered calibrated
source bundle before applying the final RMG cap.}
\label{tab:capacities_train80}
\smallskip
\parbox{0.94\textwidth}{\footnotesize $^{a}$Ten RMG block identifiers
have maximum concurrency four and twelve have maximum concurrency five;
their aggregate upper bound is 100 simultaneous RMG tasks.
This exceeds the expert-validated physical upper bound of 66 implied by at
most three RMGs at each of 22 blocks.}
\end{table}

All 168 weekly slots in each resource calendar are open. Gate closures are
handled by the arrival calendar. Every RMG activity can select any of the 22
block identifiers. The learned rules provide the weights. Removing T22 leaves
21 identifiers and removes three capacity units from the final model.
The HO2 and LL concurrency values, fully open resource calendars, and
interchangeable-block assignment mechanism were not covered by the expert
confirmation and remain model abstractions.

The raw RMG capacities sum to 100. The expert-validated physical ceiling is 66:
three RMGs at each of 22 blocks. This mismatch is important. The discovered values
describe overlapping event intervals. They are not verified crane counts.
The cap removes the implausible upper bound. It does not prove that 66 is the
effective capacity of the terminal.

This is the material expert-supported domain correction in the executable parameter bundle.
Preserving the ITS schedule-derived XES order and checking for parallel operators corrects and
guards the discovery input without replacing the discovered control flow. The
resource cap, by contrast, changes the learned capacities to enforce a stated
physical upper bound. The final simulator therefore combines an automatically
discovered Petri net and parameters with one explicit resource-domain
constraint.

An RMG block is still only a selectable resource label. It is not container
location state. After T22 is removed, the model can assign work elsewhere
without checking storage or travel feasibility. This limit is central to
Scenario~A and is discussed in Section~\ref{sec:limitations}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Sensitivity of Transition-Time Baselines}
\label{sec:results_percentile_sens}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

An older pipeline version used route-specific transition baselines to
reconstruct enablement. Section~\ref{sec:lifecycle_events} explains why this
was replaced by ProSiT's own enablement. The old analysis is kept as a
sensitivity check. It shows how much an assumed baseline changes with the
chosen percentile. It does not separate movement, traffic, queueing, or other
unrecorded delay.

The check uses percentiles $\{5, 10, 25, 50\}$ and compares them with p5.
Table~\ref{tab:percentile_sens} covers the 48 transitions with at least twenty
observations. The reproducible table contains 24 gate-to-yard and 24
yard-to-gate route pairs. The reported association is Pearson correlation.

\begin{table}[htbp]
\centering
\setlength{\tabcolsep}{3pt}
\begin{tabular}{cccccc}
\toprule
\textbf{Percentile} &
\textbf{Mean (s)} &
\textbf{Median (s)} &
\textbf{Mean shift (s)} &
\textbf{P90 shift (s)} &
\textbf{Pearson $r$ vs.\ p5} \\
\midrule
p5   & 225.8 & 223.5 & $0.0$   & $0.0$   & 1.000 \\
p10  & 254.6 & 254.3 & $28.8$  & $36.0$  & 0.990 \\
p25  & 318.3 & 311.0 & $92.5$  & $110.3$ & 0.948 \\
p50  & 435.5 & 426.0 & $209.8$ & $250.9$ & 0.812 \\
\bottomrule
\end{tabular}
\caption{Empirical transition duration under alternative percentiles
(seconds; 48 transitions with $\ge 20$ observations).}
\label{tab:percentile_sens}
\end{table}

At p5, the route-specific values range from 129.8 to 317.0 seconds, or 2.16 to
5.28 minutes. The mean and median are both about 3.7 minutes. The p5 statistic
is therefore a lower-tail operational baseline rather than a zero-duration
assumption: even low-delay observations contain a fairly stable, non-trivial
transition component. The values also keep a similar ordering across
percentiles. Pearson $r$ is 0.990 between p5 and p10 and 0.812 between p5 and
p50. Their absolute level still changes. From p5 to p50, the mean baseline
rises by 209.8 seconds, or about 3.5 minutes.

Route-frequency weighting gives a p5 baseline of 232.5 seconds for the inbound
gate-to-yard leg and 224.8 seconds for the outbound yard-to-gate leg. Their sum
is 457.2 seconds, or 7.62 minutes per visit. This is close in scale to the
7.629-minute mean turnaround under-prediction in
Table~\ref{tab:final_kpis}. The comparison is diagnostically important: omitted
physical transition time can plausibly account for a large part of the mean
bias. It is not an additive correction or a causal decomposition. The p5 values
come from a descriptive source table, use a lower-tail percentile as a proxy,
and may still combine movement, traffic, operational delay and timestamp
resolution. Internal movements in visits with several yard stops would add
further unobserved structure rather than make the two boundary components
disappear.

Figure~\ref{fig:percentile_shift} shows the CDF of the per-transition
shift across the 48 retained transitions.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.75\textwidth]{figures/discovery/percentile_shift.png}
\caption{CDF of the per-transition duration shift relative to the 5th
percentile, over the 48 transitions with at least twenty observations.}
\label{fig:percentile_shift}
\end{figure}

\medskip

\noindent\textbf{Interpretation.} The lower-tail route values are evidence that
time between recorded activities is physically meaningful and too large to be
treated as zero. They do not identify its mechanism. The data do not separate
movement, traffic, queueing, and other delay, and the real log has no
enablement timestamp. This is consistent with the simulation-guided
model-repair distinction \cite{SimulationGuidedRepair2025} between repairing duration or
inter-activity time and first resolving resource-driven versus exogenous
waiting. The present model output is consequently called pre-service delay; it
is not validated queueing time.

The raw timetable source contains 433 positive stop-to-stop observations for
multi-stop visits. No individual internal route pair reaches the twenty-case
threshold used for the route table. A route-specific internal-movement model
would therefore be unstable. Pooling those observations gives a p5 of
186.8 seconds (3.11 minutes), a p10 of 237.8 seconds (3.96 minutes), and a
median of 558 seconds (9.30 minutes). The pooled lower tail supports a
non-zero internal movement component, but the sparse route cells do not
support a matrix of yard-to-yard distributions. This is why the present
analysis uses the boundary routes as a scale check and does not insert an
unvalidated duration for every possible loop transition.

Hard-coding these values into silent Petri-net transitions is not a valid
shortcut in the present engine. ProSiT fires silent transitions immediately;
they do not hold a token for a sampled duration. A timed movement step would
therefore require either a visible auxiliary transition retained in the
simulation log or an extension of the simulator's firing semantics. More
importantly, the current pre-service distribution was fitted to the complete
observed inter-activity gap. Adding a transit draw on top would count part of
the same time twice.

A defensible extension would first estimate a non-negative, route-dependent
movement component and then refit the residual:
\begin{equation}
D_{\mathrm{pre\text{-}service}}
=T_{\mathrm{transit}}(r_{\mathrm{from}},r_{\mathrm{to}},z)
+D_{\mathrm{access/queue}}(s,z),
\end{equation}
where $z$ contains only state available at the transition and $s$ is the
current process/resource state. The components need not be statistically
independent: traffic can affect both movement and queue arrival. Independence
would be a simplifying hypothesis to test, not a property implied by the log.
The existing p5 values can initialise or bound the movement component, but
trajectory or position data are required to identify it. State-dependent
transition weights in an SLDPN decide what fires; they cannot supply this
missing timed movement mechanism by themselves
\cite{Vinci2023Inf.Activ.Prob}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Held-Out Bottleneck Analysis}
\label{sec:results_bottlenecks}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

A bottleneck analysis must distinguish a long individual activity from a
frequent activity at which a large amount of system time accumulates. The
held-out log is therefore decomposed at event level. Recorded handling service
is completion minus start. The non-negative gap from the previous event's
completion to the current event's start is called \emph{composite pre-service
delay}. For the sequential truck process, the sum of both components exactly
reconstructs each case turnaround. Composite pre-service delay can contain
movement, traffic and transfer, dispatching, and queueing; it is not interpreted
as a direct queue measurement.

Across the 17,892 held-out visits, mean turnaround is 39.742 minutes. Recorded
handling contributes 14.755 minutes per visit and composite pre-service delay
contributes 24.987 minutes. The decomposition error is zero for every case.
Table~\ref{tab:bottleneck_burden} ranks activities by their frequency-weighted
contribution rather than their mean duration alone.

\begin{table}[htbp]
\centering
\setlength{\tabcolsep}{4pt}
\begin{tabular}{L{3.2cm}rrr}
\toprule
\textbf{Activity} & \textbf{Service (min)} &
\textbf{Composite pre-service (min)} & \textbf{Total share} \\
\midrule
Gate Out     & 0      & 201,690 & 28.36\,\% \\
RMG receive  & 98,808 & 100,205 & 27.99\,\% \\
RMG delivery & 53,028 & 56,829  & 15.45\,\% \\
LL delivery  & 39,868 & 21,651  & 8.65\,\% \\
HO2 receive  & 23,671 & 20,934  & 6.27\,\% \\
\bottomrule
\end{tabular}
\caption{Largest accumulated service and composite pre-service burdens in the
held-out period. Minutes are totals over all 17,892 visits.}
\label{tab:bottleneck_burden}
\end{table}

The largest component is the gap before Gate Out: 11.273 minutes per visit on
average and 201,690 minutes in total. It contains the outbound transition and
any unobserved release or exit delay, so calling Gate Out a queueing bottleneck
would be too strong. RMG receive is the largest handling-related accumulation
point. Its mean service is 13.779 minutes and its mean composite pre-service
delay is 13.974 minutes; together the activity accounts for 27.99\,\% of all
reconstructed held-out time. RMG delivery accounts for a further 15.45\,\%.
These frequency-weighted results make the RMG receive/delivery distinction
operationally important even though the present scenarios do not identify a
resolved turnaround effect.

\texttt{LL\_mixed} is not a system-level bottleneck in this decomposition. Its
eleven held-out occurrences accumulate 233 minutes of service and composite
pre-service delay, only 0.0328\,\% of total held-out turnaround. All are followed
directly by \texttt{Gate Out}. In the training period, the six
\texttt{LL\_mixed}--\texttt{RMG\_receive} transitions have a mean downstream
pre-service gap of 10.667 minutes, compared with 11.251 minutes for
\texttt{RMG\_receive} after other predecessors. Their medians are both six
minutes and their P90 values are 22.5 and 23.0 minutes. The available evidence
therefore does not indicate additional congestion after \texttt{LL\_mixed}.
Its process-structural distinctiveness should not be confused with a large
frequency-weighted time burden.

\begin{figure}[htbp]
\centering
\includegraphics[width=0.86\textwidth]{figures/results_bottleneck_time_burden.png}
\caption{Accumulated held-out time by activity, separated into recorded
handling service and composite pre-service delay.}
\label{fig:bottleneck_time_burden}
\end{figure}

The resource-overlap audit reinforces the modelling caveat. Several RMG block
labels exhibit four overlapping recorded intervals even though the physical
scenario model caps effective capacity at three. The overlap is evidence about
the event-log resource abstraction and timestamp behaviour, not proof that a
fourth crane exists. A causal bottleneck claim requires spatially sticky block
assignment and an explicit queue state. The present analysis therefore
identifies where time accumulates and which components deserve further
modelling; it does not attribute all accumulated delay to capacity shortage.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{What-If Experiments}
\label{sec:results_scenarios}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The primary scenario baseline is the selected \textit{visit-only} bundle with
the expert-validated maximum of three available resources per RMG block. Ten
matched seeds (42--51) are run for the baseline, T22 removal, and the 20\,\%
demand increase, giving 30 simulations with 17,892 cases each. The same design
is repeated with the investigated but rejected \textit{both} bundle. This
second set is a model-choice robustness check, not a second reference model.

Table~\ref{tab:scenario_kpis} uses the baseline from this matched runner. It is only the reference for paired changes. The RQ1 results still come from Table~\ref{tab:final_kpis}.

The Inductive-Miner net also stays fixed. The experiments can change arrival
timing, resource assignment, and model-derived delays, while the loop may still
generate unseen sequential combinations of the known yard activities. Extra
activity types, detours represented by new events, and unmodelled exception
mechanisms remain outside the model. All scenario results are conditional on
this discovered control-flow language.

The operational question is not simply whether a parameter can be changed.
For terminal planning, the relevant question is how a forecast condition and a
candidate response propagate through process executions: for example, whether
reducing the number of truck slots released per hour under forecast high
utilisation protects turnaround, or whether a resource policy shifts delay from
receive to delivery handling. The scenario KPIs are therefore reported both at
case level and separately for the principal RMG handling patterns. This makes
the experiments decision-oriented while retaining the distinction between an
effect inside the simulation and an effect established for the physical
terminal.

\begin{table}[htbp]
\centering
\footnotesize
\setlength{\tabcolsep}{3.2pt}
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
\textbf{KPI} & \textbf{Reference} & \textbf{Outcome} & \textbf{Change} & \textbf{95\,\% CI} \\
\midrule
Mean turnaround (min) & 32.114 & 32.224 & $+0.110$ & [$-0.057$, $+0.277$] \\
P90 turnaround (min) & 54.200 & 54.500 & $+0.300$ & [$-0.289$, $+0.889$] \\
Mean RMG service (min) & 11.073 & 11.009 & $-0.064$ & [$-0.155$, $+0.027$] \\
Mean RMG pre-service (min) & 8.155 & 8.273 & $+0.118$ & [$+0.041$, $+0.195$] \\
RMG receive service (min) & 11.353 & 11.288 & $-0.066$ & [$-0.172$, $+0.041$] \\
RMG receive pre-service (min) & 8.198 & 8.332 & $+0.134$ & [$+0.039$, $+0.228$] \\
RMG delivery service (min) & 8.833 & 8.756 & $-0.077$ & [$-0.208$, $+0.054$] \\
RMG delivery pre-service (min) & 8.124 & 8.233 & $+0.108$ & [$-0.004$, $+0.221$] \\
Mean T22 assignments per run & 590.5 & 0.0 & $-590.5$ & exact \\
\bottomrule
\end{tabularx}
\caption{Paired Scenario A results: T22 resource-pool reallocation
($N_{\text{seeds}}=10$; scenario minus matched baseline).}
\label{tab:scenario_kpis}
\end{table}

\begin{table}[htbp]
\centering
\footnotesize
\setlength{\tabcolsep}{3.0pt}
\begin{tabularx}{\textwidth}{@{}Xrrrr@{}}
\toprule
\textbf{KPI} & \textbf{Reference} & \textbf{Outcome} & \textbf{Change} & \textbf{95\,\% CI} \\
\midrule
Model mean working inter-arrival (min) & 0.6830 & 0.5692 & $-0.1138$ & exact \\
Model arrivals per working hour & 87.842 & 105.411 & $+20.000\,\%$ & exact \\
Observed mean inter-arrival (min) & 0.8282 & 0.7145 & $-0.1137$ & [$-0.1145$, $-0.1128$] \\
Observed arrivals per elapsed hour & 72.452 & 83.979 & $+11.527$ ($+15.910\,\%$) & [$+11.478$, $+11.577$] \\
Observed simultaneous-arrival share (\%) & 54.221 & 60.015 & $+5.795$ pp & [$+5.736$, $+5.853$] pp \\
Mean turnaround (min) & 32.114 & 32.080 & $-0.033$ & [$-0.171$, $+0.104$] \\
Mean RMG service (min) & 11.073 & 11.060 & $-0.013$ & [$-0.080$, $+0.054$] \\
Mean RMG pre-service (min) & 8.155 & 8.097 & $-0.058$ & [$-0.130$, $+0.014$] \\
RMG receive service / pre-service (min) & 11.353 / 8.198 & 11.350 / 8.137 & $-0.003$ / $-0.061$ & [$-0.120$, $+0.114$] / [$-0.142$, $+0.020$] \\
RMG delivery service / pre-service (min) & 8.833 / 8.124 & 8.792 / 8.054 & $-0.040$ / $-0.070$ & [$-0.181$, $+0.101$] / [$-0.200$, $+0.059$] \\
\bottomrule
\end{tabularx}
\caption{Paired Scenario B results: 20\,\% higher working-time arrival
intensity ($N_{\text{seeds}}=10$; scenario minus matched baseline).}
\label{tab:demand_scenario_kpis}
\smallskip
\parbox{0.97\textwidth}{\scriptsize Pre-service denotes the model-derived
interval from enablement to activity start, not pure queueing time.
``Observed'' arrival measures use the minute-resolved simulated event log and
elapsed calendar time. Intervals quantify paired seed-wise variation
conditional on the frozen model. All 30 primary logs pass the structural case
contracts. One T22-closure visit in seed 46 has a turnaround of 2,050 minutes;
it is retained and reported as a duration-tail diagnostic, not treated as a
physical contract violation. No service event exceeds 24 hours. The
expert-validated RMG ceiling reduces aggregate maximum concurrency from the
discovered 100 to 66.}
\end{table}

\subsection{Scenario A: T22 Resource-Pool Reallocation}

T22 receives 590.5 assignments per baseline run. This is 4.32\,\% of RMG
events. After removal, it receives none. The intervention executes as
intended. The mean-turnaround change is
$+0.110\;[-0.057,+0.277]$ minutes. Its paired interval includes zero. Mean RMG
service changes by $-0.064\;[-0.155,+0.027]$ minutes, whereas mean RMG
pre-service delay increases by $+0.118\;[+0.041,+0.195]$ minutes.

Receive service changes by $-0.066\;[-0.172,+0.041]$ minutes and receive
pre-service increases by $+0.134\;[+0.039,+0.228]$ minutes. Delivery service
changes by $-0.077\;[-0.208,+0.054]$ minutes and delivery pre-service by
$+0.108\;[-0.004,+0.221]$ minutes. The selected model therefore resolves a
small increase in model-derived pre-service delay, concentrated in receive
handling, but no end-to-end or service-time effect. The single 2,050-minute
visit occurs in this scenario and is retained; the unresolved P90 change
($+0.300\;[-0.289,+0.889]$ minutes) shows that it is an isolated model tail,
not a general distributional shift. Removing an eligible statistical resource
is observable, whereas a physical block-closure effect is not established.

The experiment is not a full block-closure study. The remaining 21 blocks stay
eligible for all RMG activities. Each keeps capacity three. The selector moves
T22 work to another statistical resource. It does not check container
location, block compatibility, relocation, or added travel. The result only
tests the abstract resource pool.

\subsection{Scenario B: 20\,\% Higher Demand}

The working-time intervention is exact. Mean inter-arrival time changes from
0.6830 to 0.5692 minutes. The implied rate rises from 87.842 to 105.411 arrivals
per working hour, or 20\,\%. The rate measured over elapsed calendar time rises
by 15.910\,\%. Closed gate periods remain fixed. Timestamps are also floored to
minutes. This flooring turns more short positive intervals into simultaneous
observations. It does not change the zero mass in the input distribution.

Mean turnaround changes by $-0.033\;[-0.171,+0.104]$ minutes, mean RMG service
by $-0.013\;[-0.080,+0.054]$ minutes, and mean RMG pre-service by
$-0.058\;[-0.130,+0.014]$ minutes. All intervals include zero. The receive and
delivery service and pre-service intervals also include zero. Thus the
selected workload-blind reference transmits the intervention to arrival times
but not to a resolved handling or end-to-end response. This is expected to be
possible in ProSiT: changing arrivals alone need not affect service draws when
the selected trees retain no dynamic workload input. Any response would have
to arise through shared-resource scheduling and model-derived enablement.

The missing congestion response is itself a model result. Under the capped
reference, 20\,\% higher demand does not create enough system interaction to
raise turnaround. The model has no explicit queue state, container location,
or ship-side crane allocation. It cannot show that the real terminal could
absorb 20\,\% more demand without congestion.

\subsection{Sensitivity to the Expert-Validated RMG Ceiling}
\label{sec:results_rmg_cap_sensitivity}

Before the final feature-source selection, the fully contextualised
Inductive-Miner scenario experiment was repeated without replacing the
overlap-derived RMG capacities. Table~\ref{tab:rmg_cap_sensitivity} reports
matched-seed differences between its cap-three and uncapped versions.
The first contrast is a direct level effect within each setting. The second is
the interaction defined in Chapter~4: whether the cap changes a scenario
response after removing its baseline level effect.

\begin{table}[htbp]
\centering
\small
\setlength{\tabcolsep}{3.5pt}
\begin{tabular}{L{3.2cm}rrrr}
\toprule
\textbf{Setting} & \textbf{$\Delta$ mean TA} & \textbf{95\,\% CI} &
\textbf{Cap$\times$scenario} & \textbf{95\,\% CI} \\
\midrule
Baseline & $+0.085$ & [$-0.058$, $+0.228$] & -- & -- \\
T22 removal & $+0.104$ & [$-0.022$, $+0.230$] & $+0.019$ & [$-0.157$, $+0.195$] \\
20\,\% demand & $-0.017$ & [$-0.139$, $+0.106$] & $-0.102$ & [$-0.276$, $+0.073$] \\
\bottomrule
\end{tabular}
\caption{Paired RMG-cap sensitivity for mean turnaround (TA), in minutes
($N_{\text{seeds}}=10$). Direct effects are cap three minus uncapped;
interactions compare the corresponding scenario effects.}
\label{tab:rmg_cap_sensitivity}
\end{table}

Every mean-turnaround interval includes zero. Baseline RMG pre-service changes
by only $+0.011\;[-0.042,+0.065]$ minutes under the cap, and the corresponding
scenario interactions are $+0.062\;[-0.065,+0.188]$ minutes for T22 removal
and $-0.052\;[-0.178,+0.073]$ minutes for demand. The only resolved
component-level interaction is demand-side mean RMG service at
$+0.087\;[+0.013,+0.161]$ minutes. It shows that this small conditional
service response depends on the capacity representation; it does not identify
a physical cause. Although the direct T22 P90 level effect is resolved at
$+0.500\;[+0.123,+0.877]$ minutes, its cap-by-scenario interaction is not
($+0.200\;[-0.457,+0.857]$). The expert ceiling is therefore retained as a
documented physical correction. This historical diagnostic shows that it did
not drive the headline effects within that contextualised bundle; it is not an
uncapped validation of the later selected \textit{visit-only} reference.

\subsection{Capacity Pressure of the Demand Intervention}

The independent offered-load diagnostic adds an important qualification to
that interpretation. Across the 21 RMG blocks observed in the held-out period,
T13 has the highest nominal utilisation under a ceiling of three concurrent
tasks: 0.422 at the held-out arrival rate and 0.506 after multiplying arrivals
by 1.20. If block routing and service times remained unchanged, the smallest
arrival multiplier required for any block to reach mean saturation would be
2.37, not 1.20. Figure~\ref{fig:rmg_capacity_pressure} shows the complete
block-level comparison.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.92\textwidth]{figures/results_rmg_capacity_pressure.png}
    \caption{Held-out RMG offered load under the three-task block ceiling and
    the mechanical 20\,\% arrival-rate increase. The diagnostic holds routing
    and service times fixed and therefore measures scenario pressure, not
    queueing delay.}
    \label{fig:rmg_capacity_pressure}
\end{figure}

The intervention is therefore not a severe mean-capacity stress test in the
pooled block abstraction. This helps explain why the resource ceiling alone
does not generate a large turnaround response. It does not prove that queues
are absent. Twelve blocks contain at least some held-out minutes with more
than three overlapping recorded RMG events; for T13 the share is 9.82\,\%.
Those bursts may reflect genuine short-period pressure, minute-resolution
artifacts, waterside/landside allocation, or an incorrect block-pool
abstraction. The 20\,\% result alone therefore cannot distinguish an
intervention that is too modest from a final reference model that is unable to
propagate capacity pressure. A historical demand-saturation ladder provides a
separate diagnostic of whether an earlier ProSiT bundle can produce such a
mechanism. It does not identify the saturation point of the final
\textit{visit-only} bundle.

\subsection{Demand-Saturation Stress Test}

Table~\ref{tab:demand_saturation_ladder} reports a 60-run response surface from
the earlier fully contextualised diagnostic bundle. It is retained as
mechanistic evidence about that model version and is not used to validate or
select the final reference. The configured multiplier scales working-time
inter-arrivals; the realised rate ratio is measured over elapsed calendar
time. These quantities diverge because the gate calendar remains fixed while
the simulated horizon becomes shorter. All 60 runs preserve the case count,
case boundary and no-overlap contracts, and none produces a service or
turnaround duration above 24 hours.

\begin{table}[htbp]
\centering
\scriptsize
\setlength{\tabcolsep}{2.8pt}
\begin{tabularx}{\textwidth}{@{}rrrrXrX@{}}
\toprule
\textbf{Configured} & \textbf{Realised} & \textbf{Arrivals} & \textbf{Mean} & \textbf{Paired turnaround change} & \textbf{RMG pre-} & \textbf{Paired pre-service change} \\
$\boldsymbol{m}$ & \textbf{rate ratio} & \textbf{per hour} & \textbf{turnaround} & \textbf{[95\,\% CI]} & \textbf{service} & \textbf{[95\,\% CI]} \\
\midrule
1.0 & 1.000 & 72.452  & 31.627 & reference & 7.898  & reference \\
1.2 & 1.159 & 83.979  & 31.633 & $+0.006$ [$-0.136$, $+0.147$] & 7.907  & $+0.009$ [$-0.072$, $+0.090$] \\
1.5 & 1.378 & 99.855  & 32.203 & $+0.576$ [$+0.074$, $+1.078$] & 8.593  & $+0.694$ [$+0.075$, $+1.314$] \\
2.0 & 2.367 & 171.472 & 36.272 & $+4.644$ [$+3.808$, $+5.481$] & 13.776 & $+5.878$ [$+4.888$, $+6.868$] \\
2.4 & 2.825 & 204.697 & 37.341 & $+5.714$ [$+5.503$, $+5.926$] & 15.106 & $+7.208$ [$+6.975$, $+7.440$] \\
3.0 & 3.504 & 253.884 & 37.787 & $+6.160$ [$+5.889$, $+6.431$] & 15.665 & $+7.767$ [$+7.548$, $+7.985$] \\
\bottomrule
\end{tabularx}
\caption{Demand-saturation response of the earlier fully contextualised
cap-three model ($N_{\mathrm{seeds}}=10$). Times and paired
changes are in minutes; the realised rate ratio uses each level's mean elapsed
arrival rate relative to the matched baseline.}
\label{tab:demand_saturation_ladder}
\end{table}

The 20\,\% level remains unresolved for mean and P90 turnaround and for RMG
pre-service delay. At the configured 1.5 level, corresponding to a realised
rate ratio of 1.378, the first small resolved response appears: mean turnaround
rises by 0.576 minutes and mean RMG pre-service by 0.694 minutes. The decisive
change occurs at configured $m=2.0$. Because of the fixed gate calendar, this
level produces a realised elapsed-rate ratio of 2.367, almost exactly the 2.37
mean-saturation estimate. Mean turnaround then rises by 4.644
[$3.808$, $5.481$] minutes, P90 by 10.700 [$8.437$, $12.963$] minutes and mean
RMG pre-service by 5.878 [$4.888$, $6.868$] minutes.

At realised rate ratios 2.825 and 3.504, the mean-turnaround changes reach
5.714 and 6.160 minutes, and the corresponding P90 changes reach 13.600 and
14.800 minutes. By contrast, mean RMG service changes by only 0.025
[$-0.060$, $0.109$] and 0.040 [$-0.035$, $0.115$] minutes at these two levels.
The high-load response is therefore carried by model-derived pre-service
delay rather than longer service draws. Figure~\ref{fig:demand_saturation_ladder}
shows this non-linear response and places the independent 2.37 offered-load
estimate on the realised-rate axis.

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.96\textwidth]{figures/results_demand_saturation_ladder.png}
    \caption{Response of the earlier fully contextualised diagnostic model to increasing
    realised elapsed arrival rates. Points are ten-seed means and shaded bands
    are 95\,\% Monte-Carlo intervals. The dashed line marks the independent
    2.37 nominal mean-saturation estimate for the most-loaded held-out RMG
    block.}
    \label{fig:demand_saturation_ladder}
\end{figure}

For the earlier fully contextualised bundle, the result demonstrates an
abstract capacity-pressure mechanism. Once realised demand reaches the independently
estimated capacity region, pre-service and end-to-end delay increase clearly.
The missing response at 20\,\% is therefore primarily evidence that the policy
scenario was a modest stress inside this model, not evidence that ProSiT can
never generate congestion. It does not establish why the final
\textit{visit-only} model produces its null response. The stress curve is still not a physical CTB
forecast. Its onset and shape depend on interchangeable block assignments,
fully open discovered resource calendars and model-derived enablement rather
than verified spatial queues. The expert-validated three-RMG ceiling supports
the capacity bound; it does not validate these propagation semantics. Even at
the 3.504 realised rate ratio, mean turnaround remains below the observed
hold-out mean of 39.74 minutes. Artificial overload therefore cannot replace
the omitted transition-time and spatial mechanisms needed for baseline
fidelity.

\subsection{Scenario Response Across State Configurations}

The selected \textit{visit-only} and rejected \textit{both} bundles are
compared with the same cap, seeds and interventions. In \textit{both}, T22
removal changes mean turnaround by $+0.072\;[-0.000,+0.145]$ minutes and mean
RMG pre-service by $+0.056\;[+0.012,+0.100]$ minutes. Its receive pre-service
change is also resolved, while delivery service falls by
$0.095\;[0.006,0.184]$ minutes. The 20\,\% demand intervention changes mean
turnaround by $+0.057\;[-0.075,+0.188]$ minutes; mean RMG service and
pre-service remain unresolved. These component results differ from the
selected reference even though both configurations retain the same Petri net.

The matched difference-in-differences is more important than whether one
configuration happens to cross a 95\,\% threshold. For mean turnaround, the
\textit{both} response minus the \textit{visit-only} response is
$-0.038\;[-0.220,+0.143]$ minutes for T22 removal and
$+0.090\;[-0.096,+0.276]$ minutes for higher demand. Neither excludes zero.
The corresponding mean RMG service and pre-service contrasts are also
unresolved. Two narrower outcomes are bundle-sensitive: for higher demand,
the P90 response differs by $+0.500\;[+0.123,+0.877]$ minutes and receive
pre-service by $+0.137\;[+0.013,+0.261]$ minutes. Thus the absence of a mean
turnaround response is robust, whereas tail and component conclusions are not
fully stable across plausible parameter bundles. This is direct evidence of
input-model uncertainty that within-configuration Monte-Carlo intervals alone
do not show.

\medskip

\noindent\textbf{RQ4 answer.} Both policy interventions execute as specified. T22
assignments fall to zero, and working-time arrival intensity rises by exactly
20\,\%. In the selected \textit{visit-only} reference, both mean-turnaround
intervals include zero. T22 removal produces a small resolved increase in
model-derived RMG pre-service delay, especially for receive handling; the
demand intervention produces no resolved handling or end-to-end mean response.
The isolated 2,050-minute T22 visit and bundle-sensitive P90 contrasts reinforce
that these are model experiments rather than stable physical effects. The
capacity-pressure audit
also shows that 20\,\% higher arrivals leave the most-loaded held-out block at
only 0.506 nominal mean utilisation under the three-task ceiling. The extended
demand ladder shows that an earlier fully contextualised bundle can propagate
abstract capacity pressure. At a realised elapsed-rate ratio of
2.367, mean turnaround rises by 4.644 minutes and mean RMG pre-service by 5.878
minutes, and stronger levels produce still larger responses. Because the
ladder does not use the final \textit{visit-only} bundle, it neither explains
that model's null response nor establishes its saturation threshold. The
threshold and propagation mechanism of the earlier bundle have also not been
validated as physical CTB behaviour.
That capability is the operational reason for simulation: candidate truck-slot
rates and handling policies can be compared before they are applied. The
present model, however, does not support a physical counterfactual or an
operational recommendation from these results because its spatial assignment,
movement and queue semantics are not yet adequate. Executability and even a
plausible stress response are not counterfactual validity.

The earlier matched uncapped sensitivity reaches the same headline conclusion
within the fully contextualised bundle: no direct cap effect or cap-by-scenario
interaction on mean turnaround is resolved. It supports retaining the ceiling
as a physical correction, but it does not prove that the final
\textit{visit-only} response would be unchanged without the cap. Its resolved
cap-by-demand interaction in mean RMG service is reported as historical model
sensitivity rather than physical evidence.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Interpretation: From Process Mining to Data-Aware Simulation}
\label{sec:results_interpretation}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

The results become most informative when the structural, temporal and
scenario findings are interpreted together. They do not describe one model
that is simply ``good'' or ``bad.'' Instead, they show that different forms of
process-mining evidence answer different questions. The compact Inductive-Miner
net reproduces the held-out control flow while generalising across sequential
yard combinations, the arrival model reproduces an important part of the
workload timing, and the simulator remains too fast at the level of complete
truck visits. These findings are compatible because control-flow conformance,
arrival fidelity, and end-to-end temporal fidelity are distinct properties
\cite{Camargo2021,ChapelaCampa2023Trust,Chapela2025SimQuality}.

\paragraph{Meaning for traditional Process Mining}
Traditional Process Mining is strongest in this study as a language for
reconstructing and checking observed process behaviour. Here, its most
important diagnostic is not merely the near-perfect held-out fitness. It is the
discovery that the apparent parallelism vanished when the ITS schedule-derived
stop order stored in XES was preserved. The earlier parallel model represented a timestamp-based
reordering artefact; the final process tree has no parallel operator. This
demonstrates a classical Process-Mining point with direct simulation
consequences: the event-log ordering relation is part of the model input, and a
formally sound discovery can still encode the wrong semantics when coarse
timestamps are treated as an unambiguous order
\cite{PMRozinat2009,PMvanDerAalst2016}.

The final model makes the fitness--precision--generalisation trade-off visible.
It fits 99.994\,\% of held-out traces and retains a held-out generalisation of
0.9413, including behaviour not stored as an exact training path. Precision is
lower at 0.7558 because the loop admits additional yard combinations and
repetitions. In this case, that broader language is not equivalent to physical
parallelism and is not automatically an error: the terminal process allows
varying sequential activity orders. The domain check must therefore be
semantic and operator-specific. Penalising every unseen order would collapse
discovery into memorisation, while accepting a parallel cut would violate the
truck-visit contract.

The silent gate-only path makes the same trade-off concrete. It is permitted
by the discovered language and retained as an auditable finding rather than
removed through an undocumented guard. None of the four standardized
decision-tree configurations samples it in 178,920 visits per configuration;
an earlier unconditional-routing benchmark sampled it in 0.059\,\% of visits.
Formal language analysis and realised-output validation therefore answer
complementary questions. A permitted path need not be common, yet it remains
relevant when the model is interpreted beyond the observed period.

This is a precision-side model-repair problem. The simulation-guided
control-flow repair framework \cite{SimulationGuidedRepair2025} provides a rigorous way to
identify differences between real and simulated control flow and to compare an
original and repaired net on held-out data. Its published operators currently
add missing behaviour, however, whereas the CTB bypass requires restricting
excess behaviour. A state-aware stochastic Petri-net study
\cite{Vinci2023Inf.Activ.Prob} demonstrates that an explicit manual restriction before simulation can be defensible when a
source model permits a minimal trace absent from the log. For CTB, such a
restricted net should therefore be evaluated as a separate robustness model,
not substituted silently for the discovered model. The rare realised frequency
does not by itself make the formal defect harmless.

The same separation applies to time. Good control-flow coverage does not
account for the 7.629-minute mean turnaround deficit or the 16.800-minute P90
deficit. The simulator can put activities in the correct order and still omit
time between them. In the CTB setting, this missing time may contain movement,
traffic, resource interaction, queueing or operational states that are not
represented as events. The temporal bias is therefore not a contradiction of
the conformance result; it identifies a different abstraction boundary. This
is why evaluating an event-log-derived simulator only through replay fitness
or variant frequencies would have produced an overly positive conclusion.

The transition-baseline diagnostic makes the scale of that abstraction
boundary concrete. Route-specific p5 values lie between 2.16 and 5.28 minutes,
and the frequency-weighted inbound and outbound lower-tail components sum to
7.62 minutes per visit. Their proximity to the 7.629-minute mean deficit is
consistent with omitted physical movement being a major contributor. It does
not prove that all residual time is travel, because the baseline also contains
unseparated traffic and operational delay and because a percentile summary
cannot be subtracted case by case. The fair conclusion is therefore stronger
than merely saying that ``some time is missing,'' but weaker than claiming a
validated travel-time mechanism.

\paragraph{Meaning for data-aware Process Mining}
Recent data-aware methods extend conventional discovery by conditioning
routing, duration or resource behaviour on case attributes and process state
\cite{mannhardt2023stochasticProcessesModelling,Vinci2023Inf.Activ.Prob,Vinci2026prosit}.
The CTB results show both the value and the limit of that extension. Its value
is selective rather than universal. Calendar context contains a strong and
stable signal for arrivals: the time-aware benchmark reduces arrival-count
MAE by 67.5\,\% and RMSE by 51.2\,\%, and the day arrival rate is about 8.7
times the night rate. In this part of the process, conditioning on information
available at case creation captures a real predictive regularity.

The duration results point in the other direction. For the two dominant RMG
activities, the diagnostic decision trees have slightly negative held-out
$R^2$. The factorial experiment makes the source of the endpoint result
clearer. Static workload proxies worsen activity-frequency and 3-gram fidelity;
native state raises turnaround EMD and interacts negatively with the static set,
while neither source resolves a weighted service-time improvement. The
clean-static model retains no demand or utilisation split and still changes the
selected visit/location tree structure. Conditional branches may therefore be
interpretable without adding stable later-period predictive information. This
is not proof that workload never matters; it shows that the available CTB
representations do not transfer as useful state in this experiment.

This qualification is central to the interpretation of data awareness.
Adding attributes increases representational capacity, but useful capacity
depends on information timing, measurement quality and a relationship that is
stable across the training and application regimes. Case-level target demand
or utilisation may be correlated with an observed duration without measuring
the physical mechanism that caused it. Conversely, a weak marginal
relationship can hide omitted event-level states such as the actual queue,
truck position or equipment movement. The white-box character of ProSiT is
valuable here because the learned conditions can be inspected and challenged;
interpretability makes the association auditable, but does not convert it into
causality or guarantee predictive validity
\cite{Vinci2026prosit,Sargent2013Validation}.

This result sharpens rather than contradicts earlier process-state-abstraction
experiments \cite{Vinci2023Inf.Activ.Prob}. The best-performing abstraction
varies between the two evaluated processes, and an unsuitable history state can
create problematic simulated behaviour. The CTB ablation supplies the missing
application-specific test: the parsimonious visit/process state reproduces
local control flow most closely, whereas neither workload source improves the
dominant timing error. It also goes beyond data-aware routing alone. The framework of
López-Pintado et al. \cite{lopez2024} demonstrates the value of case data for
control-flow decisions but identifies data-dependent activity duration as a
separate extension. CTB shows why that separation matters in physical
logistics: state-dependent firing probabilities cannot by themselves create
travel, queues, or spatially constrained service.

The resource result is equally consistent with the differentiated-resource
evidence of López-Pintado and Dumas \cite{BPSdifferentiatedResources2022}.
Treating resources as statistically interchangeable can preserve event counts
while distorting timing. In CTB, capping nominal RMG concurrency does not make
target assignment spatially sticky and does not model crane or truck movement.
By contrast, physical container-terminal simulation represents travel,
equipment interaction and allocation policies explicitly
\cite{Kastner2021}. The current hybrid research opportunity is therefore to
retain the discovered white-box process and state rules while supplying a
mechanistic movement, queue, and resource-assignment layer.

\paragraph{Correlation, scenarios, and operational meaning}
The results therefore distinguish predictive association from intervention
response. The temporal arrival features show a strong association with when
trucks enter the terminal, but the 20\,\% arrival intervention produces no
resolved mean-turnaround increase. This combination does not imply that real
CTB turnaround is independent of demand. It shows that the fitted simulator
contains an arrival relationship, while the offered-load calculation places
the chosen change below mean block saturation. The historical ladder shows
that an earlier fully contextualised model develops a strong pre-service and
turnaround response when realised arrivals reach the independently estimated
capacity region. This is supporting mechanism evidence, not a direct
explanation of the final model. In the same way, eliminating T22
assignments verifies that the resource rule was
changed, while the near-zero turnaround effect reflects reallocation within an
abstract interchangeable resource pool. It is not evidence that closing a
physical block would have no operational effect.

Taken together, the scenario results act as stress tests of the model's
semantics. A null effect can be a substantive operational finding only when
the mechanisms through which the intervention should propagate are represented
and validated. Here, the saturation experiment demonstrates a capacity-driven
response inside the abstract resource model, but it does not validate the
physical route through which that response would arise. Location feasibility,
movement, differentiated crane availability and spatial queue state remain
absent. The contribution of the
data-aware white-box model is consequently twofold: it supports reproducible
and inspectable changes, and it makes the boundary between an implemented
association and a credible physical mechanism visible. That boundary is the
main result for decision support. The model is suitable for analysing
conditional consequences inside its documented abstraction, but not yet for
claiming causal terminal effects from the scenarios or stress ladder.

The target application nevertheless remains concrete. Before publishing truck
appointments, a terminal could combine a utilisation forecast with candidate
hourly slot limits, simulate each policy, and compare turnaround together with
receive- and delivery-specific service and pre-service KPIs. If forecast
conditions threaten retrieval performance, a candidate policy could reduce
the admitted truck rate or prioritise retrieval-compatible resources; the
simulator would quantify both the intended benefit and any transferred delay
to delivery operations. This prospective comparison is the benefit that a
validated logistics simulation should provide. The CTB experiments establish
the transparent experimental interface and show exactly which additional
queue, location, and equipment states must be represented before that interface
can be used prescriptively.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Layered Validity Synthesis}
\label{sec:results_layered_validity}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Table~\ref{tab:validity_ladder} brings the diagnostics together. It prevents a
strong result in one perspective from being used to imply validity in another.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{3.0cm}L{2.4cm}X}
\toprule
\textbf{Validity layer} & \textbf{CTB assessment} & \textbf{Decisive evidence} \\
\midrule
Event and case semantics & Supported & ITS schedule-derived stop order stored in XES and no case-contract violations in accepted runs. \\
Control-flow language & Supported with caveat & Fitness 0.999992 and no parallel operator; precision 0.7558 and a formal gate-only bypass. \\
Historical stochastic fidelity & Mixed & Arrival and yard-service distances are moderate; mean and tail turnaround remain too short. \\
Temporal transfer & Not stable & Real mean turnaround rises by 2.450 minutes and all yard-service means increase. \\
Resource and spatial semantics & Not supported physically & The cap limits concurrency but does not enforce target location, travel, queue, or equipment movement. \\
Scenario response & Responsive, not physically validated & Policy inputs change reproducibly; a non-linear pre-service and turnaround response appears around the nominal saturation region. \\
Prescriptive counterfactual use & Not yet supported & Intervention mechanisms and stakeholder plausibility require further validation. \\
\bottomrule
\end{tabularx}
\caption{Layered validity assessment of the CTB simulation.}
\label{tab:validity_ladder}
\end{table}

The boundary is systematic. Traditional Process Mining supports the first two
layers particularly well: it reveals ordering semantics, generalisation, and
excess language. Data-aware simulation adds auditable conditional routing and
timing rules and supports the third layer where suitable state is available.
The later layers require mechanisms that are not implied by correlation alone.
Recent work is moving in the same direction: evolving-process discovery
addresses drift \cite{vinci2025onlinediscoverysimulationmodels}, and temporal
deviation analysis seeks to attribute simulation mismatch to arrival,
processing, and resource inputs rather than reporting magnitude alone
\cite{Wang2026TemporalDeviations}. The CTB study contributes a physical-
logistics instance in which those layers fail at different points.

This framing also clarifies the role of expert intervention. A hospital
simulation-discovery application combines automatic discovery with manual
structural changes and expert plausibility assessment before optimisation
\cite{Hospital2026Vinci}. The CTB structural repair experiment follows the same
principle of explicit, separately evaluated domain knowledge. A comparable
expert validation of queue, travel and resource semantics remains necessary
before slot-rate or handling-priority recommendations are presented as terminal
counterfactuals.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Methodological Limitations}
\label{sec:limitations}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\paragraph{Pre-Service Delay}

ProSiT enables an activity when the process model allows it. Consecutive CTB
activities can occur at different locations, but truck movement is not a
separate activity in the event-log design. The time from completion of one
activity to the start of the next can therefore combine
\begin{equation}
    D_{\mathrm{pre\text{-}service}} \approx
    T_{\mathrm{physical}} +
    T_{\mathrm{traffic}} +
    T_{\mathrm{queue}} +
    T_{\mathrm{other}}.
\end{equation}
The log cannot identify these parts separately. The percentile check shows
that a reconstructed baseline is sensitive to its assumption. It does not
measure any one part. This would require position, trajectory, queue-entry, or
similar state data. These data are not available. The results use
the term \textit{pre-service delay}, not queueing time.

\paragraph{Resource Assignment Is Not Spatial Container Relocation}

An RMG resource in ProSiT is an abstract capacity assignment. The discovered
overlap capacity is 100. The stated physical cap is 66 across 22 blocks. This
mismatch shows that timestamp overlap is not a crane count. The selected
scenario reference model uses the cap of three per block.

The cap does not add spatial state. Target-area attributes are predictors, not
hard inventory constraints. The simulator does not bind a container to a
storage block. It also does not create relocation work after reassignment.
Closing T22 spreads its statistical demand across the remaining blocks. It
does not model feasibility, crane movement, or added truck travel. Scenario~A
tests the resource-pool abstraction. It does not forecast a physical T22
closure.

\paragraph{Scope of Expert Validation}

The project-level expert review confirmed the gate boundaries, the requirement
for at least one yard activity, arbitrary yard ordering and repetition, the
validity of \texttt{LL\_mixed}-only yard visits and the invalidity of gate-only
visits. It also confirmed two hard physical constraints. One truck does not
execute activities in parallel, and no block can use more than three RMGs
concurrently. These judgments support the case contract, operator check,
bypass diagnosis and cap-three correction. They do not validate the discovered
HO2 or LL capacities, the fully open resource calendars, arrival and duration
distributions, block-interchangeability assumption, pre-service-delay
semantics, or any scenario response. The review was a requirements check and
did not produce a formal transcript or inter-rater assessment. Its evidential
scope is therefore narrower than a structured expert-validation study.

\paragraph{The Empirical Population Excludes Multi-RMG Visits}

The case-level ProSiT representation requires one unambiguous RMG target for
the block-specific context variables. The pipeline therefore removes 1,693
visits and 6,820 events involving more than one RMG block. The results apply to
the resulting one-RMG subprocess, not to the complete population of CTB truck
movements. The exclusion removes some of the visits most likely to contain
additional movement, cross-block resource interaction and spatial complexity.
It may therefore make transport and congestion behaviour easier than in the
full terminal population. An object-centric representation could preserve the
truck, containers and multiple blocks as related objects instead of forcing one
block value onto the complete visit
\cite{VanDerAalst2023OCPM,Knopp2023ObjectCentricSimulation}. This is a direct
empirical scope boundary, not only a general future-work opportunity.

\paragraph{Scenario Control Flow Generalises Only Within a Fixed Activity Set}

The Inductive-Miner loop can generate sequential combinations and repetitions
that were not present as complete training variants. This is less restrictive
than the earlier trie representation, but it is not an open-ended terminal
model. A scenario cannot create a new activity type, an unrecorded exception
mechanism, or a structurally different response to disruption. The two policy
experiments and the demand ladder therefore test parameter changes inside the
discovered activity language. Missing structural mechanisms may still remove
real sources of delay.

\paragraph{Systematic Turnaround Under-Prediction}

The data-aware reference model under-predicts mean turnaround by 7.629 minutes and P90 by
16.800 minutes. The narrow seed intervals show that this is model bias, not
simulation noise. The log cannot show how much missing time comes from
movement, traffic, queueing, or an unrecorded state. Correct case order does
not remove this limit on KPI forecasts.

\paragraph{Scenario Intervals Cover Only Part of Structural Uncertainty}

The matched-seed intervals in Tables~\ref{tab:scenario_kpis},
\ref{tab:demand_scenario_kpis}, and
\ref{tab:demand_saturation_ladder} cover random
variation in one fixed model. They do not cover a new training period, another
discovery run, or missing spatial state. The cap sets a ceiling but adds no
location feasibility or equipment movement. More seeds would narrow these
intervals. They would not reduce structural uncertainty. The matched
state-configuration comparison addresses one part of that uncertainty: it
shows that the unresolved mean-turnaround response persists across the selected
\textit{visit-only} and rejected \textit{both} bundles. It still does not cover
a new training period, another discovered net,
alternative resource semantics, or the missing spatial and queueing state.

\paragraph{External Validity Is Limited to One Terminal and Time Window}

The temporal hold-out is one later period from the same terminal and source
systems. Near-perfect replay fitness shows structural stability in this
period, but the observed 2.450-minute mean-turnaround shift shows that temporal
stationarity does not hold even within the study window. The results do not
cover another terminal, season, gate regime, or infrastructure setup. That
would require another full discovery and validation run; an adaptive or
rolling-window simulator would additionally require prospective evaluation.
