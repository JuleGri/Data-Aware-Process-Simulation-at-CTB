\chapter{Case Study and Data Engineering}
\label{chp:case_study_and_data}

This chapter describes the operational context and the pipeline that turns raw operational
records into a simulation-ready event log. It covers the data engineering but does not yet fit models, evaluate results, or choose experimental parameters. The resulting event log is the input for Chapters~\ref{chp:experiments_and_framework} and~\ref{chap:results}.

Section~\ref{sec:cs_environment} introduces HHLA Container Terminal
Burchardkai (CTB), the truck-handling operations and the data sources.
Section~\ref{sec:event_log_engineering} describes how the pipeline reconstructs lifecycle timestamps and derives the case-level context required by ProSiT
\cite{Vinci2026prosit}. The final section describes the event log and its characteristics used for the train/test split in Section~\ref{sec:train_test_split}.

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Case Study Environment}
\label{sec:cs_environment}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

    \subsection{HHLA Container Terminal Burchardkai}

    This case study concerns the HHLA Container Terminal Burchardkai (CTB) in the Port of Hamburg. CTB handles approximately 2.6 million TEU
    (Twenty-foot Equivalent Units) annually across 315 hectares
    \cite{hhla2025}.

    \textbf{Truck process.} External trucks visit CTB to deliver or receive
    containers. At \textit{Gate In}, the truck is registered at the entry
    gate. It then drives to the assigned yard position, where an RMG or other
    handling equipment loads or unloads the container. After all planned
    operations are completed, the truck leaves through \textit{Gate Out}.

    The CTB environment in this project has several resource constraints.
    Located between the waterside and the landside, 22 RMG storage
    blocks (T06--T27) serve both sides. Operationally, at most three
    automated gantry cranes can work in parallel at one block. The CTB
    has two specialised areas: an empty-container depot (called
    ``MT'' or ``LL'', from the German \textit{Leerlager}) and the
    manually operated HO2 yard area. The terminal gate consists of 16
    lanes, split between incoming and outgoing trucks. The number of
    operated gate lanes varies by day, shift and terminal resource plan.

    \subsection{Data Sources}
    \label{sec:cs_data_sources}

    The data come from several systems used in daily terminal operations.
    These systems were developed over time and record different parts of the
    process. A Terminal Operating System coordinates them.
    Table~\ref{tab:ctb_data_sources} summarises the four sources used in this
    thesis.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{4.4cm}Y}
\toprule
\textbf{Data source} & \textbf{Data provided} \\
\midrule
ITS schedule & Truck-visit schedules and timestamps for gate entry,
handling readiness, order completion and gate exit. \\
Report system & Stage-level execution data, gate-entry records,
exchange-area assignments, container transactions and carrier identifiers. \\
Yard management system & Four-hour snapshots of storage-block capacity,
occupancy and utilisation. \\
Container master data & Container size, type, loading status, hazardous and
reefer indicators, dwell time and inbound/outbound carriers. \\
\bottomrule
\end{tabularx}
\caption{Operational data sources used for event-log engineering.}
\label{tab:ctb_data_sources}
\end{table}

    These systems generate independent data pools with different
    identifier schemes, requiring a data-integration procedure
    described in Section~\ref{sec:event_log_engineering}. Historical
    data from March and April 2026 (approximately eight weeks) were extracted.
    The raw extract covers approximately 104,000 truck visits, 113,466
    container move events and 336 yard-state snapshots
    ($8$ weeks $\times 7$ days $\times 6$ snapshots per day).

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
\section{Event Log Engineering}
\label{sec:event_log_engineering}
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

Event-log engineering is the first stage of the experimental pipeline.
The objective is to construct an integrated, process-oriented event log
from the raw operational data that satisfies the requirements of both
process mining and data-aware simulation while retaining as much
operational context as possible. This section describes the pipeline
stage by stage.

Figure~\ref{fig:event_log_engineering_pipeline} summarises how the four
operational data sources are transformed into the final simulation-ready
event log.

\begin{figure}[htbp]
\centering
\includegraphics[width=\textwidth]{figures/schemas/event_log_engineering_pipeline_bw.png}
\caption{Event-log engineering pipeline. The ITS schedule, report-system
records, yard snapshots and container master data are cleaned and integrated.
Truck visits are then represented as cases with lifecycle events and
case-level context. The final checks produce the CSV and XES event logs used
for discovery and evaluation.}
\label{fig:event_log_engineering_pipeline}
\end{figure}

\subsection{Raw Data Sources and Preparation}

\paragraph{ITS Schedule Data}

    The ITS scheduling system provides the scheduling data for the
truck operations reconstructed in this thesis. Each record represents
one activity instance within a truck visit. The attributes used by the
pipeline are listed in Table~\ref{tab:its_source_attributes}.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{5.9cm}Y}
\toprule
\textbf{Attribute} & \textbf{Description} \\
\midrule
FAHRPLAN\_UID & Unique truck-visit identifier used as the case ID. \\
LKW\_KENNZEICHEN & Truck licence plate number. \\
ANLAUFPUNKT\_HALTESTELLE & Exchange-area identifier, such as T20,
LL or VC. \\
FP\_ZEITPUNKT\_ERSTELLUNG & Time at which the schedule entry was
created; used as the Gate In timestamp. \\
ALP\_ZEITPUNKT\_BEREITMELDUNG & Time at which the handling activity
was reported ready for execution. \\
ALP\_ZEITPUNKT\_ERFUELLUNG & Time at which the handling activity was
completed. \\
FP\_ZEITPUNKT\_GATEOUT & Time at which the truck exited the
terminal. \\
\bottomrule
\end{tabularx}
\caption{ITS schedule attributes used in the event-log pipeline.}
\label{tab:its_source_attributes}
\end{table}

All timestamp fields are parsed into standardised \texttt{datetime}
objects using \texttt{pandas} with the format
\texttt{\%d.\%m.\%Y~\%H:\%M}. Invalid or missing timestamps are flagged
for removal during the cleaning phase.

\paragraph{Report System Data}

The report system provides complementary stage-level execution data,
completing the details for gate-entry events, and it links each
container number to the specific truck visit at a yard handling point.
The attributes used from this source are shown in
Table~\ref{tab:report_source_attributes}.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{5.9cm}Y}
\toprule
\textbf{Attribute} & \textbf{Description} \\
\midrule
TruckVisitGkey & Truck-visit identifier used for cross-system
matching. \\
TruckLicenseNbr & Truck licence plate number. \\
CallupTime & Time at which the truck physically entered the
terminal. \\
StageStartTime & Start time recorded for the reported process
stage. \\
StageId & Stage identifier, such as OCR, Gate-In, yard or Exit. \\
TruckExchangeArea & Yard handling point visited for a specific
container. \\
TranCtrNbr & Container number associated with the transaction. \\
UnitInboundCarrierId & Inbound carrier identifier, recorded as a
truck plate, vessel voyage number or train ID. \\
UnitOutboundCarrierId & Outbound carrier identifier, recorded as a
truck plate, vessel voyage number or train ID. \\
\bottomrule
\end{tabularx}
\caption{Report-system attributes used in the event-log pipeline.}
\label{tab:report_source_attributes}
\end{table}

A container is considered ``picked up'' or ``delivered'' by a truck if
the corresponding inbound or outbound carrier identifier resolves to a
truck. The report system uses detailed handover-lane codes for the
exchange area; these must be mapped to the higher-level operational
zones used by the scheduling system. All exchange areas
beginning with ``HP'' (\textit{Hochpalette}) are mapped to ``LL''
(\textit{Leerlager}) to align with the schedule system's terminology.
The mapping is implemented in
\texttt{002\_Prep\_area\_mapping\_report.py}.

\paragraph{Container Master Data}

Container-level attributes are extracted from the container master data
and linked to events through the matched \texttt{container\_id}.
Table~\ref{tab:container_master_attributes} describes the retained fields and
derived measures.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{4.6cm}Y}
\toprule
\textbf{Attribute} & \textbf{Description} \\
\midrule
Container size & TEU-equivalent size category (20, 40 or 45 feet). \\
Full/empty status & Binary loading-status indicator (Leer/Voll). \\
Reefer status & Indicator for refrigerated containers, derived from the ISO
code. \\
Hazardous-material status & Indicator derived from the IMCO code. \\
Container type & Operational type, such as standard, open-top, platform or
bulk. \\
Dwell time & Hours already spent in the terminal for a receive operation. \\
Container complexity score & Composite measure based on size, loading status,
reefer and hazardous-goods requirements, container type and dwell time. \\
\bottomrule
\end{tabularx}
\caption{Container-master attributes used for context enrichment.}
\label{tab:container_master_attributes}
\end{table}

The complexity score is derived during the container-master-data
preprocessing stage and combines container type, loading status,
hazardous-goods indicators, reefer requirements and other
handling-related properties into a single numerical measure. Higher
values indicate visits expected to require more complex handling.

\paragraph{Yard State Data}

Yard state data provides time-series information on storage-block
capacities and utilisation rates. Each record contains
\texttt{ZEITPUNKT} (observation timestamp), \texttt{AREA} (storage area
identifier), \texttt{KAP\_TEU\_THEORETISCH} (theoretical TEU capacity),
\texttt{BEL\_TEU} (current TEU occupancy) and \texttt{AUSLASTUNG\_\%}
(utilisation percentage). Because observations are irregularly spaced,
the data is resampled to a uniform one-minute frequency using linear
interpolation on the numeric fields (script
\texttt{004\_Prep\_create\_interpolated\_yard\_state\_for\_s3.py}).

\paragraph{Truck Demand Data}

Truck demand information is derived from the scheduling system by
aggregating truck arrivals over rolling time windows. For each exchange
area and one-minute time interval, the feature
\texttt{TRUCK\_DEMAND\_LAST\_1H} is calculated as the number of trucks
that became ready for processing in that area during the preceding
sixty minutes. The demand pipeline (script
\texttt{005\_Prep\_create\_truck\_demand.py}) first resamples the schedule
data to one-minute intervals based on
\texttt{ALP\_ZEITPUNKT\_BEREITMELDUNG}. It then applies a rolling
60-minute window and counts truck arrivals separately for the Gate, the
individual RMG blocks T06--T27, VC, LL and MT. The block-level RMG series are
also aggregated into one yard-section demand measure.

The resulting demand features capture workload pressure at the time of
case arrival. They are included as contextual predictors because such
pressure may influence waiting times and resource-allocation decisions;
their empirical contribution is evaluated under RQ3 in
Section~\ref{sec:simulation_discovery_methodology}.

\FloatBarrier

\subsection{Data Cleaning and Integration}
\label{sec:data_cleaning}

\paragraph{Cross-System Identifier Matching}

The scheduling and report systems use different identifiers for truck
visits. To create a unified event log they are linked through composite
keys. Truck visits are matched by
$(\texttt{TruckLicenseNbr}, \texttt{CallupTime})\equiv
(\texttt{LKW\_KENNZEICHEN}, \texttt{FP\_ZEITPUNKT\_ERSTELLUNG})$
using a left join that preserves all schedule records and augments them
with the report-system \texttt{TruckVisitGkey} when available. Container
numbers are then matched via
$(\texttt{TruckVisitGkey}, \texttt{TruckExchangeArea})\equiv
(\texttt{TruckVisitGkey}, \texttt{ANLAUFPUNKT\_HALTESTELLE})$.

When the first matching step fails, a fallback is
applied to single-exchange-area visits: if a truck visit contains only
one yard activity and no container identifier was matched, the
container ID is inferred from any other transaction associated with the
same \texttt{TruckVisitGkey}. The integration logic is implemented in
\texttt{01\_match\_container\_ids\_to\_schedule.py}.

\paragraph{Invalid Process Instances}
\label{sec:invalid_process_instances}

The cleaning pipeline removes complete cases rather than individual
records: whenever a truck visit violates one of the defined quality
criteria the entire process instance is excluded, so that only
internally consistent process executions are retained. The
cleaning procedures are implemented in
\texttt{00\_its\_fahrplan\_raw\_to\_fahrplan\_cleaned\_0304.py}.

\paragraph{Missing lifecycle information.} Cases are removed if one or
more required lifecycle timestamps are missing:
\texttt{FP\_ZEITPUNKT\_ERSTELLUNG},
\texttt{ALP\_ZEITPUNKT\_BEREITMELDUNG} or
\texttt{ALP\_ZEITPUNKT\_ERFUELLUNG}. Such records cannot be transformed
into valid lifecycle events. The quality report identified 1,267
affected cases ($\approx 1.34\%$ of all truck visits).

\paragraph{Temporal inconsistencies.} Cases are removed when
temporal constraints are violated. First, activities with
negative execution durations
($\texttt{ALP\_ZEITPUNKT\_ERFUELLUNG}<\texttt{ALP\_ZEITPUNKT\_BEREITMELDUNG}$)
indicate timestamp errors and are excluded. Second, overlapping
activities within a single visit ($\text{start}_{i+1}<\text{complete}_{i}$
after sorting) contradict the operational logic (a truck can only be at
one handling location at a time). The cleaning procedure identified
three cases with negative durations and 43 with overlapping activities.

\paragraph{Extreme runtime outliers.} Two further runtime filters are
applied. The first removes cases with excessive gate-out delay
($t_{\text{GateOut}}-t_{\text{LastCompletion}}>10\text{h}$); such
records commonly indicate system failure. The second removes visits
whose total turnaround time exceeds twelve hours
($t_{\text{GateOut}}-t_{\text{GateIn}}>12\text{h}$), which is
operationally implausible. The filters identified 22 extreme gate-out
cases and 22 twelve-hour outliers.

\paragraph{Process Completeness Filters}

\paragraph{Container identification coverage.} Overall, 111,646 of
113,136 operational records could be assigned a valid
\texttt{TruckVisitGkey} (98.68\%), and 110,866 records could be assigned
a valid container identifier (97.99\%). Despite the fallback procedure,
2,270 records belonging to 2,034 truck visits remained without a valid
container assignment. Since container-related attributes are needed for
the later context enrichment and simulation-discovery stages, these
process instances are removed entirely (a complete-case filter). This
removes 2,034 truck visits and 3,397 associated records, leaving 91,335
complete truck visits and 109,739 operational records.

\paragraph{Unknown activity types.} During activity construction, cases
containing activities classified as \texttt{unknown} are removed. Such
events cannot be mapped to operational process steps with enough confidence.
They would introduce ambiguity into process discovery and simulation discovery.

\paragraph{Process scope restriction.} The event log is restricted to
truck visits containing exactly one interaction with an automated RMG
block. Visits may still contain additional activities in other yard
areas (empty-container yard or VC yard), but only a single RMG block
interaction is permitted. This restriction ensures that RMG-specific
workload, utilisation and demand measures can be assigned one value per case.
It forms the basis for the
block-focused what-if experiments in
Section~\ref{sec:what_if_design}.

\paragraph{Event-level cleanup.} Immediately before exporting the event
log, remaining events with an \texttt{\_unknown} suffix are removed.
Unlike the previous filters, this is an event-level cleanup rather than
a case-level exclusion; its purpose is to remove residual events
that cannot be interpreted within the final process model.

\subsection{Case and Activity Definition}
\label{sec:case_definition}

A \textit{case} in the event log corresponds to a single truck visit,
uniquely identified by \texttt{FAHRPLAN\_UID}. Each case represents the
complete sequence of activities executed by one truck between gate
entry and gate exit.

The data-aware simulation framework used in this thesis
\cite{Vinci2026prosit} accepts only case-level contextual attributes.
To attach block-specific context features
(\texttt{target\_utilization}, \texttt{target\_demand},
\texttt{target\_rank}) consistently, the design decision was made to
include only process instances that interact with exactly one RMG block.
Visits involving multiple RMG blocks are excluded because a single value could
not be assigned to their block-specific congestion measures. Visits to
other yard areas (VC or LL) that involve exactly one RMG block are
retained. The filtering logic is implemented in
\texttt{filter\_one\_rmg\_only.py}.

This case-centric design does not discard the container characteristics that
are available for a retained truck visit; they remain available as contextual
predictors. It does, however, collapse the independent lifecycles and relations
of trucks, containers, yard blocks, and equipment into one visit-oriented
record. An object-centric event log could preserve these entities and associate
a handling event with all participating objects
\cite{VanDerAalst2023OCPM}. It could also retain multi-block visits without
forcing one block-level value onto the complete case. Such a representation
would require a different source-data mapping and an object-centric simulation
engine \cite{Knopp2023ObjectCentricSimulation}; it is therefore outside the
scope of the ProSiT-based experiment rather than an untested variant of the
same input configuration.

\paragraph{Activity Construction}
\label{sec:activity_construction}

Activities are defined by combining an \textit{operation type}
(receive, delivery or mixed) with a \textit{resource type} (an RMG
block, LL or HO2). Table~\ref{tab:activities} lists the resulting
labels.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{4.2cm}Y}
\toprule
\textbf{Activity} & \textbf{Definition} \\
\midrule
Gate In       & Truck registration at entry gate \\
RMG\_receive  & RMG unloads container from truck (import) \\
RMG\_delivery & RMG loads container onto truck (export) \\
RMG\_mixed    & Both receive and delivery at same block \\
LL\_receive   & Empty-container depot: unload \\
LL\_delivery  & Empty-container depot: load \\
LL\_mixed     & Empty-container depot: unload and load \\
HO2\_receive  & Manual area: unload \\
HO2\_delivery & Manual area: load \\
HO2\_mixed    & Manual area: unload and load \\
Gate Out      & Truck departure \\
\bottomrule
\end{tabularx}
\caption{Activity labels used in the final event log.}
\label{tab:activities}
\end{table}

The 22 individual RMG blocks (T06--T27) are retained as distinct
resources to preserve block-specific utilisation patterns.

\subsection{Activity Timestamps and Temporal Semantics}
\label{sec:lifecycle_events}

The CTB operational systems provide timestamps from which two observed
lifecycle states are constructed for each activity. They are summarised in
Table~\ref{tab:observed_lifecycle_attributes}.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{4.6cm}Y}
\toprule
\textbf{Attribute} & \textbf{Description} \\
\midrule
start:timestamp & Observed or reconstructed time at which execution
of the activity began. \\
time:timestamp & Observed or reconstructed time at which execution
of the activity finished. \\
\bottomrule
\end{tabularx}
\caption{Observed lifecycle timestamp attributes in the final event log.}
\label{tab:observed_lifecycle_attributes}
\end{table}

The \textit{service time} (or execution time) is the directly
identifiable temporal quantity:
\begin{equation}
    ET_i = t_{\mathrm{complete},i} - t_{\mathrm{start},i}.
\end{equation}

An explicit \texttt{enabled:timestamp} is \textbf{not} reconstructed
in the input event log.  Consistent with ProSiT
\cite{Vinci2026prosit} and earlier simulation-discovery work
\cite{Camargo2020,PMRozinat2009}, activity enablement is obtained from
process-model alignment during the simulation-parameter discovery
phase (Section~\ref{sec:simulation_discovery_methodology}).  ProSiT
uses an A*-based optimal alignment as defined in
Section~\ref{sec:astar_alignment_definition} to relate each observed
event to a transition and determine when that transition becomes enabled
in the discovered Petri net, then computes a model-derived pre-service
delay relative to both the enablement time and the resource's
availability schedule.

\paragraph{Timestamp Mapping by Activity Type}

Table~\ref{tab:activity_timestamp_mapping} shows how the lifecycle timestamps
are mapped for each activity type.

\begin{table}[htbp]
\centering
\begin{tabularx}{\textwidth}{L{2.2cm}YY}
\toprule
\textbf{Activity type} & \textbf{Start timestamp} &
\textbf{Complete timestamp} \\
\midrule
Gate In & FP\_ZEITPUNKT\_ERSTELLUNG & Same value as the start
timestamp. \\
Yard activity & ALP\_ZEITPUNKT\_BEREITMELDUNG &
ALP\_ZEITPUNKT\_ERFUELLUNG \\
Gate Out & FP\_ZEITPUNKT\_GATEOUT & FP\_ZEITPUNKT\_GATEOUT \\
\bottomrule
\end{tabularx}
\caption{Lifecycle timestamp mapping by activity type.}
\label{tab:activity_timestamp_mapping}
\end{table}

The OCR gate timestamp (\texttt{StageStartTime}) from the report system is
retained in the auxiliary column \texttt{ocr\_timestamp} for diagnostic
purposes. It is not mapped to activity enablement.

\paragraph{Model-Derived Pre-Service Delay}

Because activity enablement is derived from the process model, the
resulting quantity
\begin{equation}
    D_{\mathrm{pre\text{-}service},i}
    = t_{\mathrm{start},i} - t_{\mathrm{enabled,model},i}
\end{equation}
is a \textit{model-derived pre-service delay}.  In ProSiT terminology
this is called ``waiting time,'' but in the CTB application it must
not be interpreted as pure queueing or resource-induced waiting.
Physical truck movement between consecutive terminal locations is not
represented as a separate activity in the discovered process model.
The model-derived interval may therefore combine physical movement between
locations, internal traffic and congestion, resource availability and queue
waiting, and other delays not represented by the control-flow model.
This is a domain-specific limitation when transferring
business-process simulation semantics to physical terminal operations
and is a methodological limit of this thesis
(discussed further in Section~\ref{sec:limitations}).

\paragraph{Methodological Note: The Abandoned Transition-Baseline Approach}
\label{sec:abandoned_baseline}

An earlier version of the event-log pipeline estimated lower-tail
route-specific transition durations from the scheduling data (5th
percentile of the empirical distribution per transition pair,
\texttt{transition\_baseline.csv}) and used these to reconstruct an
explicit \texttt{enabled:timestamp} in the input event log.  Closer
inspection revealed two issues. First, the statistical route baselines are
aggregate quantiles rather than event-specific observations of physical
arrival. Their values therefore depend on the selected percentile
(Section~\ref{sec:results_percentile_sens}). Second, the estimated readiness
timestamp sometimes occurred after the observed start timestamp. This
produced negative waiting times that had to be clipped to zero.
ProSiT does not require an externally constructed enabled timestamp.
It derives enablement from the aligned process model
\cite{Vinci2026prosit}. The final method keeps only the observed start and
completion timestamps and leaves enablement reconstruction to the
process-model alignment. This keeps the data and simulation semantics
consistent.

The transition-baseline data is preserved in the repository as
diagnostic material and may be useful for future work that introduces
an explicit spatial-transition model.

\FloatBarrier

\subsection{Context Enrichment}
\label{sec:context_enrichment}

To enable data-aware simulation, the event log is enriched with
contextual attributes describing the operational state at the time of
process execution. These attributes are organised into five
categories.

\paragraph{Information Available at Truck Entry}
\label{sec:entry_known_attributes}

The scheduling record contains the complete planned visit manifest before
the truck enters the terminal. The number of planned stops,
the numbers of delivery and receive transactions, the planned target area
and the resulting final visit-complexity score are known at Gate In. The
pipeline reproduces this entry-time information by aggregating all rows of
the same \texttt{FAHRPLAN\_UID}; it does not infer these attributes from
later realised durations or completion outcomes.
\texttt{visit\_complexity} is a deterministic summary of the planned
stop/container manifest (visit size, container-handling complexity and
mixed-flow complexity), not an ex-post performance measure. Using these
variables as case attributes does not leak the simulated future
provided that the same planned manifest is supplied for a prospective run.

The implementation detail that the target block is reconstructed from the
planned stop's area-specific activity label can otherwise look like
look-ahead: after genericising \texttt{T06--T27} activities, the reconstructed
block is copied into \texttt{primary\_target\_area}. Semantically, however,
the source is the planned \texttt{ANLAUFPUNKT\_HALTESTELLE} attached to the
visit order and available at entry, not the later activity-completion
timestamp. This distinction defines the deployment contract for all
entry-known case attributes.

\paragraph{Container Features}

Container-related context is aggregated to case level so that visit
properties can serve as case-level attributes in ProSiT (script
\texttt{03\_aggregate\_container\_details\_case\_level.py}). The resulting
\texttt{n\_containers} attribute counts the unique containers handled during
the visit. The binary attributes \texttt{has\_hazardous} and
\texttt{has\_reefer} are aggregated using a logical OR across the complete
manifest. The \texttt{full\_ratio} records the share of loaded containers,
while \texttt{container\_complexity\_mean} and
\texttt{container\_complexity\_sum} describe the average and cumulative
handling complexity of the visit.

\paragraph{Yard Features}

Yard state features capture the storage capacity and utilisation status
of different terminal areas at truck arrival (Gate In event). These
features are matched temporally using \texttt{pd.merge\_asof} with a
\texttt{nearest} strategy. The joined snapshot provides the overall
\texttt{gate\_utilization}, the aggregated \texttt{rmg\_utilization}, and
the area-level \texttt{vc\_utilization} and \texttt{mt\_utilization}. For
single-block visits, \texttt{target\_utilization} gives the utilisation of
the assigned RMG block.

These features are added in \texttt{06\_add\_utils\_to\_eventlog.py}.

\paragraph{Demand Features}

Demand features quantify operational load pressure at different
terminal areas over a rolling 60-minute window (scripts
\texttt{05\_add\_demand\_features\_to\_eventlog.py} and
\texttt{08\_add\_all\_target\_features.py}). The general
\texttt{gate\_demand} counts trucks processed across the terminal during the
preceding hour. The \texttt{rmg\_demand}, \texttt{vc\_demand} and
\texttt{mt\_demand} attributes separate this load by operational area.
For single-block visits, \texttt{target\_demand} records the demand for the
assigned RMG block.

\paragraph{Target Block Features}

For single-block truck visits, the assigned target RMG block is required for
the context model. Beyond the target-specific utilisation and demand,
\texttt{target\_rank} gives its ordinal position among the RMG blocks based
on utilisation at case arrival. The corresponding
\texttt{target\_rank\_group} reduces this rank to \texttt{top\_3},
\texttt{mid} or \texttt{low}. The attributes
\texttt{primary\_target\_area} and \texttt{target\_area} retain the assigned
block identifier.

These features are reconstructed from the planned, area-specific stop
labels before the RMG activities are genericised and are then carried as
case attributes (scripts
\texttt{07\_convert\_to\_RMG\_filter\_LL+HO2\_only.py} and
\texttt{08\_add\_all\_target\_features.py}). Their information time is
Gate In as specified above under Information Available at Truck Entry; the
later \texttt{org:resource} assignment is not used as an execution outcome
to be predicted.

\subsection{Final Event Log and Characteristics}
\label{sec:eventlog_structure}

The final event log combines process-execution data, lifecycle
information and operational context within a single simulation-ready
representation. Table~\ref{tab:eventlog_attributes} groups the process and
case-level attributes used by the simulation.

\begin{table}[p]
\centering
\renewcommand{\arraystretch}{1.02}
\begin{tabularx}{\textwidth}{@{}L{4.4cm}Y@{}}
\toprule
\textbf{Attribute} & \textbf{Description} \\
\midrule
\multicolumn{2}{c}{\textbf{Baseline attributes}} \\
\midrule
case:concept:name & Unique truck-visit identifier (FAHRPLAN\_UID). \\
concept:name & Activity executed during the visit. \\
start:timestamp & Observed or reconstructed activity start timestamp. \\
time:timestamp & Observed or reconstructed activity completion timestamp. \\
org:resource & Operational location of the activity. \\
\midrule
\multicolumn{2}{c}{\textbf{Visit-related attributes}} \\
\midrule
process\_flow\_type & Visit type: delivery, receive or mixed. \\
n\_containers & Number of containers in the visit. \\
n\_stops & Number of planned operational stops known at Gate In. \\
n\_deliveries & Number of planned delivery transactions known at Gate In. \\
n\_receives & Number of planned receive transactions known at Gate In. \\
has\_hazardous & Indicator for at least one IMDG container. \\
has\_reefer & Indicator for at least one reefer container. \\
full\_ratio & Share of loaded containers. \\
visit\_complexity & Composite score of the complete planned visit manifest. \\
\midrule
\multicolumn{2}{c}{\textbf{Demand attributes}} \\
\midrule
gate\_demand & Truck demand across the terminal during the preceding hour. \\
rmg\_demand & Truck demand in the RMG yard during the preceding hour. \\
vc\_demand & Truck demand in the VC area during the preceding hour. \\
mt\_demand & Truck demand in the manual yard during the preceding hour. \\
\midrule
\multicolumn{2}{c}{\textbf{Utilisation attributes}} \\
\midrule
gate\_utilization & Overall yard utilisation at truck arrival. \\
rmg\_utilization & Aggregate RMG-yard utilisation at truck arrival. \\
vc\_utilization & VC-area utilisation at truck arrival. \\
mt\_utilization & Manual-yard utilisation at truck arrival. \\
\midrule
\multicolumn{2}{c}{\textbf{Target attributes}} \\
\midrule
target\_utilization & Utilisation of the primary target area at truck arrival. \\
target\_demand & Demand for the primary target area during the preceding hour. \\
target\_utilization\_bin & Quantile category of target-area utilisation. \\
target\_demand\_bin & Quantile category of target-area demand. \\
target\_rank & Utilisation rank of the target among all RMG blocks. \\
target\_rank\_group & Categorised target rank: top\_3, mid or low. \\
primary\_target\_area / target\_area & Planned target area known at Gate In. \\
\bottomrule
\end{tabularx}
\caption{Process and contextual attributes passed to ProSiT.}
\label{tab:eventlog_attributes}
\end{table}

All contextual attributes are attached at case level and remain constant
throughout the case. This design is consistent with the
current ProSiT feature-discovery approach, which mainly utilises
case-level context information rather than event-level dynamic state
\cite{Vinci2026prosit}.

\paragraph{Event Log Characteristics}
\label{sec:eventlog_characteristics}

After the full preprocessing pipeline the final event log contains
89,460 truck visits represented by 275,408 events. The activity
distribution is shown in Table~\ref{tab:activity_distribution}.
Table~\ref{tab:eventlog_stats} summarises the overall event log.

\begin{table}[htbp]
\centering
\begin{subtable}[t]{.46\textwidth}
  \centering
  \begin{tabular}{@{}lr@{}}
  \toprule
  \textbf{Activity} & \textbf{Frequency} \\
  \midrule
  Gate In       & 89,460 \\
  Gate Out      & 89,460 \\
  RMG\_receive  & 39,965 \\
  RMG\_delivery & 22,504 \\
  LL\_delivery  & 13,230 \\
  HO2\_delivery & 7,224  \\
  HO2\_receive  & 5,464  \\
  RMG\_mixed    & 4,939  \\
  LL\_receive   & 2,073  \\
  HO2\_mixed    & 1,025  \\
  LL\_mixed     & 64     \\
  \bottomrule
  \end{tabular}
  \caption{Activity distribution.}
  \label{tab:activity_distribution}
\end{subtable}
\hfill
\begin{subtable}[t]{.50\textwidth}
  \centering
  \begin{tabular}{@{}lr@{}}
  \toprule
  \textbf{Statistic} & \textbf{Value} \\
  \midrule
  Cases                  & 89,460 \\
  Events                 & 275,408 \\
  Distinct activities    & 11 \\
  Observation start      & 2026-03-02 05:27 \\
  Observation end        & 2026-04-30 11:54 \\
  Distinct RMG resources & 22 (T06--T27) \\
  Additional pools       & LL, HO2, Gate In, Gate Out \\
  \bottomrule
  \end{tabular}
  \caption{Summary statistics.}
  \label{tab:eventlog_stats}
\end{subtable}
\caption{Composition and scope of the final event log.}
\label{tab:eventlog_overview}
\end{table}
\FloatBarrier

Every case contains a \texttt{Gate In} event, one or more operational
yard activities and a final \texttt{Gate Out} event. Operational
activities can occur at an RMG block, the empty area or the manual area, depending on the operational requirements of the visit.


The generated event log is exported as the full CSV
\texttt{s6\_eventlog\_target\_rank\_features.csv}. The frozen temporal partitions are exported as \texttt{s6\_train.csv},
\texttt{s6\_test.csv} and the corresponding
\texttt{xes\_files/s6\_train.xes} and
\texttt{xes\_files/s6\_test.xes}. CSV is the pipeline format; XES enables compatibility with pm4py, ProM
and ProSiT for process discovery and conformance analysis.

The chronological character of the log is preserved, as every
case follows a increasing temporal behavior from Gate In to Gate Out. This property is further examined in Chapter~\ref{chp:experiments_and_framework} to define a temporal train/test split at case level (see Section~\ref{sec:train_test_split}).
